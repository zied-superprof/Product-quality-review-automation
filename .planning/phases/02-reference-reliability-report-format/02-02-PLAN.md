---
phase: 02-reference-reliability-report-format
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - .claude/commands/review-translations.md
autonomous: true
requirements:
  - RPT-01
  - RPT-02
  - RPT-03

must_haves:
  truths:
    - "Running /review-translations with no --format flag produces both .md and .html files in reports/"
    - "Running /review-translations --format pdf additionally produces a .pdf file (requires weasyprint)"
    - "Running /review-translations --format md produces only .md (no HTML conversion)"
    - "Every report has sections in fixed order: Summary table, French reference, Grouped sections, Single-market sections, Markets with no issues, Undefined variables"
    - "Empty sections show the header followed by 'No issues found.' rather than being omitted"
    - "The French reference section is always present showing verbatim French title and body"
    - "Report filename contains the notification ID extracted from CSV content, not the CSV filename"
  artifacts:
    - path: ".claude/commands/review-translations.md"
      provides: "Step 0 --format flag, Step 6 HTML conversion, consistent section template, notification ID filename"
      contains: "--format"
  key_links:
    - from: ".claude/commands/review-translations.md Step 0"
      to: ".claude/commands/review-translations.md Step 6"
      via: "--format arg parsed in Step 0, consumed in Step 6 for output selection"
      pattern: "--format"
    - from: ".claude/commands/review-translations.md Step 6"
      to: "reports/*.html"
      via: "inline MD-to-HTML conversion using markdown library + CSS from generate_pdf.py"
      pattern: "markdown.markdown"
    - from: ".claude/commands/review-translations.md Step 1"
      to: ".claude/commands/review-translations.md Step 6"
      via: "notification ID extracted in Step 1, used for filename in Step 6"
      pattern: "review-.*notification.*id"
---

<objective>
Make reports configurable (HTML default), structurally consistent across runs, and named by notification ID.

Purpose: Non-technical teammates can open reports in any browser without Markdown knowledge; reports always have the same predictable structure; filenames identify what was reviewed.
Output: Updated review-translations.md with --format flag parsing (Step 0), consistent section template (Step 6), HTML conversion step (Step 6), and notification-ID-based filenames (Step 1 + Step 6).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-reference-reliability-report-format/02-CONTEXT.md
@.planning/phases/02-reference-reliability-report-format/02-RESEARCH.md
@.planning/phases/02-reference-reliability-report-format/02-01-SUMMARY.md

@.claude/commands/review-translations.md
@scripts/generate_pdf.py

<interfaces>
<!-- CSS template to extract from generate_pdf.py for inline HTML conversion -->
From scripts/generate_pdf.py (lines 21-131):
The full CSS block and HTML wrapper structure are at lines 16-131. The conversion call is:
```python
body = markdown.markdown(content, extensions=["tables", "fenced_code"])
```
The HTML wrapper is:
```html
<!DOCTYPE html>
<html>
<head>
<meta charset="utf-8">
<title>Translation Quality Review - By Country</title>
<style>
  body { font-family: -apple-system, 'Helvetica Neue', Arial, sans-serif; ... }
  /* Full CSS at lines 22-126 of generate_pdf.py */
</style>
</head>
<body>
{body}
</body>
</html>
```

From .claude/commands/review-translations.md Step 0 (current arg parsing):
```markdown
- `--languages XX,YY` — optional, only review these language codes
- `--notification ID` — optional, only review this notification
- `--structural-only` — optional, skip AI review and only run structural checks
```

From .claude/commands/review-translations.md Step 6 (current report template):
```markdown
### `reports/review-by-country-YYYY-MM-DD.md`
```
Current filename pattern: `review-by-country-YYYY-MM-DD.md`
New filename pattern: `review-[notification-id]-YYYY-MM-DD.md` (per D-19)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --format flag to Step 0, notification ID extraction to Step 1, update filename in Step 6</name>
  <files>.claude/commands/review-translations.md</files>

  <read_first>
    .claude/commands/review-translations.md
    scripts/generate_pdf.py
  </read_first>

  <action>
  Four changes to `.claude/commands/review-translations.md`:

  **Change 1 — Step 0: Add --format arg (per D-11, D-12, D-15):**

  In the Step 0 argument list, after `--structural-only`, add:
  ```markdown
  - `--format html|md|pdf` — optional, controls report output. Default: `html` (writes both .md and .html). `md` writes .md only. `pdf` writes .md + .html + .pdf (requires weasyprint). If an unrecognized value is passed, print: `Unknown format "[value]". Valid options: html, md, pdf` and abort.
  ```

  **Change 2 — Step 1: Add notification ID extraction (per D-19, D-20, D-21):**

  After the reference file health check paragraph (added by Plan 02-01), add:

  ```markdown
  **Notification ID extraction** — Extract the notification identifier for use in the report filename:
  1. If `--notification` was passed, use that value as the notification ID.
  2. Otherwise, look for a column in the CSV header that contains "notification" (case-insensitive). If found, use the value from the first data row.
  3. If no notification column exists, derive the ID from the CSV filename: strip the `.csv` extension, strip any path prefix.
  4. Sanitize the ID for filenames: lowercase, replace spaces and slashes with hyphens, strip characters that are not alphanumeric or hyphens.

  Store the sanitized notification ID for use in Step 6 filename generation.
  ```

  **Change 3 — Step 6: Replace filename and add HTML conversion step (per D-13, D-14, D-19):**

  Replace the current heading:
  `### \`reports/review-by-country-YYYY-MM-DD.md\``

  With:
  ```markdown
  ### Report filename and output

  **Filename**: `reports/review-[notification-id]-YYYY-MM-DD.md` (and `.html` / `.pdf` as applicable)

  Where `[notification-id]` is the sanitized ID from Step 1, and `YYYY-MM-DD` is today's date.

  **Output behavior by --format flag:**
  - `html` (default): Write the Markdown report to `reports/review-[id]-[date].md`, then convert to HTML and write to `reports/review-[id]-[date].html`. Tell the user: "Report generated at `reports/review-[id]-[date].html` (Markdown source: `.md`)"
  - `md`: Write only the Markdown report to `reports/review-[id]-[date].md`. Tell the user: "Report generated at `reports/review-[id]-[date].md`"
  - `pdf`: Write .md, convert to .html, then convert to .pdf using weasyprint. If weasyprint is not installed, print: "PDF generation requires weasyprint. Install with: pip install weasyprint" and fall back to html behavior. Tell the user about all generated files.

  **MD-to-HTML conversion step** (runs for `html` and `pdf` formats):
  After writing the .md file, run an inline Python snippet:
  ```python
  import markdown
  from pathlib import Path

  md_content = Path("[md_path]").read_text(encoding="utf-8")
  body = markdown.markdown(md_content, extensions=["tables", "fenced_code"])

  css = """
    body { font-family: -apple-system, 'Helvetica Neue', Arial, sans-serif; font-size: 11pt; line-height: 1.65; color: #1a1a2e; max-width: 800px; margin: 0 auto; padding: 32px 40px; }
    h1 { font-size: 20pt; font-weight: 700; color: #0f3460; border-bottom: 3px solid #e94560; padding-bottom: 8px; margin-top: 0; }
    h2 { font-size: 13pt; font-weight: 700; color: #ffffff; background: #0f3460; padding: 6px 12px; margin-top: 24px; border-radius: 3px; }
    h3 { font-size: 11pt; font-weight: 600; color: #0f3460; margin-top: 16px; border-left: 4px solid #e94560; padding-left: 8px; }
    h4 { font-size: 10pt; font-weight: 600; color: #333; font-style: italic; margin-top: 12px; }
    table { width: 100%; border-collapse: collapse; font-size: 9pt; margin: 12px 0; }
    th { background: #0f3460; color: white; padding: 5px 8px; text-align: left; font-weight: 600; }
    td { padding: 4px 8px; border-bottom: 1px solid #e0e0e0; vertical-align: top; }
    tr:nth-child(even) td { background: #f8f9fc; }
    blockquote { border-left: 3px solid #e94560; margin: 10px 0; padding: 5px 12px; background: #fff5f5; color: #555; font-style: italic; }
    code { background: #f0f0f0; padding: 1px 4px; border-radius: 3px; font-family: 'Courier New', monospace; font-size: 9pt; }
    pre { background: #f6f8fa; padding: 12px; border-radius: 4px; overflow-x: auto; font-size: 9pt; }
    strong { color: #0f3460; }
    hr { border: none; border-top: 1px solid #ddd; margin: 16px 0; }
    ol, ul { padding-left: 20px; }
    li { margin-bottom: 4px; }
    @media print { body { max-width: 100%; padding: 0; } h2 { -webkit-print-color-adjust: exact; print-color-adjust: exact; } th { -webkit-print-color-adjust: exact; print-color-adjust: exact; } tr:nth-child(even) td { -webkit-print-color-adjust: exact; print-color-adjust: exact; } }
  """

  html = f"<!DOCTYPE html>\n<html>\n<head>\n<meta charset=\"utf-8\">\n<title>Translation Quality Review</title>\n<style>{css}</style>\n</head>\n<body>\n{body}\n</body>\n</html>"
  Path("[html_path]").write_text(html, encoding="utf-8")
  ```
  Replace `[md_path]` and `[html_path]` with the actual report paths.
  ```

  **Change 4 — Step 6: Enforce consistent section structure (per D-16, D-17, D-18):**

  At the top of the Step 6 report template (before the markdown code block), add:

  ```markdown
  **Section order is FIXED — every report must use these sections in this exact order, regardless of findings:**

  1. **Summary table** — always present, shows all markets with their error/warning/suggestion counts (even if all zeros)
  2. **French reference** — ALWAYS present, even when the reference is clean. Shows verbatim French title and body. If Step 4a found issues, the "French reference issues" sub-section appears below; if clean, only the verbatim text is shown.
  3. **Grouped sections** — markets with identical corrections grouped together. If no groups exist, omit this section entirely (do not show an empty group header).
  4. **Single-market sections** — markets with unique corrections. If none, omit.
  5. **Markets with no issues** — always present. If every market has issues, show: `## Markets with no issues\nNo issues found.`
  6. **Undefined variables** — shown ONLY when the structural validator returned `variable_undefined` findings (per D-07). If all variables are recognized, omit this section entirely.

  **Empty section rule (per D-17):** Sections 1, 2, and 5 are always present. If section 5 has no clean markets, display `No issues found.` under the header. Sections 3, 4, and 6 are conditional — they appear only when relevant findings exist; they are not shown with "No issues found." when empty.
  ```

  Then update the existing report template markdown block to match this order. Specifically:
  - Move the `## French reference (France)` section to always be present (remove the HTML comment that says "only include if Step 4a found issues" for the verbatim title/body — the issues sub-section remains conditional)
  - Ensure the `## Markets with no issues` section has the fallback text `No issues found.` when empty
  </action>

  <verify>
    <automated>cd "/Users/juan/Desktop/Superprof Projects/Product quality review automation" && grep -c "\-\-format html|md|pdf" .claude/commands/review-translations.md && grep -c "notification-id" .claude/commands/review-translations.md && grep -c "markdown.markdown" .claude/commands/review-translations.md && grep -c "Section order is FIXED" .claude/commands/review-translations.md && grep -c "No issues found" .claude/commands/review-translations.md</automated>
  </verify>

  <acceptance_criteria>
    - `.claude/commands/review-translations.md` Step 0 contains `--format html|md|pdf`
    - `.claude/commands/review-translations.md` Step 0 contains `Unknown format` abort instruction
    - `.claude/commands/review-translations.md` Step 1 contains `Notification ID extraction` paragraph
    - `.claude/commands/review-translations.md` Step 1 contains sanitization rules: `lowercase, replace spaces and slashes with hyphens`
    - `.claude/commands/review-translations.md` Step 6 contains `review-[notification-id]-YYYY-MM-DD` filename pattern (not `review-by-country-`)
    - `.claude/commands/review-translations.md` Step 6 contains `markdown.markdown(md_content, extensions=["tables", "fenced_code"])` conversion call
    - `.claude/commands/review-translations.md` Step 6 contains the full CSS block from generate_pdf.py (at minimum: `font-family: -apple-system` and `#0f3460` and `#e94560`)
    - `.claude/commands/review-translations.md` Step 6 contains `Section order is FIXED`
    - `.claude/commands/review-translations.md` Step 6 contains `No issues found.` fallback text
    - `.claude/commands/review-translations.md` Step 6 French reference section comment does NOT say "only include if" for the verbatim title/body display (the issues sub-section can remain conditional)
    - The old `review-by-country-YYYY-MM-DD.md` pattern is no longer present as the primary filename template
  </acceptance_criteria>

  <done>
    The review skill parses --format flag in Step 0 (default html), extracts notification ID in Step 1 for filename, generates reports with the new naming pattern, always writes .md and converts to .html by default using the CSS from generate_pdf.py, and enforces a fixed section order with empty-section fallbacks. PDF requires explicit --format pdf and weasyprint.
  </done>
</task>

</tasks>

<verification>
1. `grep "\-\-format" .claude/commands/review-translations.md` shows the format flag in Step 0
2. `grep "notification-id" .claude/commands/review-translations.md` shows the filename pattern
3. `grep "markdown.markdown" .claude/commands/review-translations.md` shows the HTML conversion step
4. `grep "Section order is FIXED" .claude/commands/review-translations.md` shows the consistency enforcement
5. `grep -c "review-by-country-" .claude/commands/review-translations.md` returns 0 (old pattern removed from primary template)
6. `grep "No issues found" .claude/commands/review-translations.md` shows empty section fallback
</verification>

<success_criteria>
- /review-translations without --format flag produces both .md and .html files
- --format md produces only .md
- --format pdf attempts .md + .html + .pdf (with weasyprint fallback message)
- Report filenames use notification ID, not "by-country"
- Every report has the fixed section order: Summary, French ref, Grouped, Single-market, No issues, Undefined vars
- French reference section is always present (even when clean)
- Empty "Markets with no issues" section shows "No issues found." instead of being omitted
- CSS in HTML output matches the generate_pdf.py template visually
</success_criteria>

<output>
After completion, create `.planning/phases/02-reference-reliability-report-format/02-02-SUMMARY.md`
</output>
