---
phase: 13-standalone-feedback-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/variables_guide.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FEEDBACK-11]
tags: [variables, documentation, tier2, seed]

must_haves:
  truths:
    - "config/variables_guide.md exists with H1 '# Variables Guide'"
    - "All 10 variable categories from label_patterns.json.variable_categories appear as H2 sections in variables_guide.md"
    - "Every variable listed in label_patterns.json.variable_categories has an H3 entry in variables_guide.md with Purpose, Valid for, Do NOT use for, Notes labels"
    - "REQUIREMENTS.md FEEDBACK section contains a FEEDBACK-11 entry"
    - "REQUIREMENTS.md Traceability table maps FEEDBACK-11 to Phase 13"
    - "ROADMAP.md Phase 13 Requirements line includes FEEDBACK-11"
  artifacts:
    - path: "config/variables_guide.md"
      provides: "Human-readable per-variable documentation guide; Tier 2 destination for variable promotions"
      contains: "# Variables Guide"
      min_lines: 300
    - path: ".planning/REQUIREMENTS.md"
      provides: "FEEDBACK-11 requirement entry + traceability mapping"
      contains: "FEEDBACK-11"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 13 Requirements line updated to include FEEDBACK-11"
      contains: "FEEDBACK-11"
  key_links:
    - from: "config/variables_guide.md"
      to: "config/label_patterns.json variable_categories"
      via: "H2 sections derived from the 10 keys"
      pattern: "^## (general|category|subject|article_contractions|compound_phrases|city|department|region|level|landing)"
    - from: "config/variables_guide.md"
      to: "config/Variables.csv"
      via: "Aide column seeds Purpose; Exemple de valeur seeds Notes tail"
      pattern: "\\*\\*Purpose:\\*\\*"
---

<objective>
Create the seed `config/variables_guide.md` — a human-readable, per-variable documentation file that becomes the Tier 2 destination for variable-related rule promotions in `/submit-feedback`. Also adds the new FEEDBACK-11 requirement to REQUIREMENTS.md and updates ROADMAP.md.

Purpose: D-07 added this file as a SCOPE ADDITION because variable-usage rules need a human-readable home that complements the machine-readable `subject_variable_usage_rules` block in `label_patterns.json`. Without this file, Tier 2 variable promotions have no destination — `/submit-feedback` cannot satisfy FEEDBACK-09 routing for variable rules.

Output: Populated `variables_guide.md` (300+ lines, 10 H2 categories, ~70 H3 variable entries seeded from `Variables.csv` + `label_patterns.json.variable_categories`) plus updated REQUIREMENTS.md and ROADMAP.md entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-standalone-feedback-skill/13-CONTEXT.md
@.planning/phases/13-standalone-feedback-skill/13-PATTERNS.md
@config/label_patterns.json
@config/Variables.csv
@CLAUDE.md

<interfaces>
<!-- Source of truth for the H2 category list (D-08 enumerates these 10) -->

From config/label_patterns.json (lines 49–181), the 10 categories that become H2 sections — in order:
1. `general` — TPL_SPACE, TPL_5_ETOILES, TPL_CDN, TPL_NDD, TPL_TAG_IMG, TPL_NB_AVIS_GLOBAL*, TPL_AVIS_SP_GLOBAL*
2. `category` — TPL_IF_CATEGORIE, TPL_CATEGORIE, TPL_CATEGORIE_NOM, TPL_CATEGORIE_ID, TPL_CATEGORIE_MINUS*, TPL_CATEGORIE_D_DE, TPL_CATEGORIE_DEMARCHES, TPL_CATEGORIE_URL
3. `subject` — TPL_IF_MATIERE, TPL_MATIERE, TPL_MATIERE_ID, TPL_MATIERE_MINUS, TPL_MATIERE_FIRST_MAJUS, TPL_MATIERE_MINUS_SMART, TPL_MATIERE_FIRST_MAJUS_SMART
4. `article_contractions` — TPL_D_DE*, TPL_A_AN*, TPL_LE_LA_LES*, TPL_DES_DE_COURS, TPL_DU_DE_DEPARTEMENT, TPL_DU_DE_REGION, TPL_D_DE_VILLE, TPL_D_DE_NIVEAU*
5. `compound_phrases` — TPL_MATIERE_DE_MATIERE, TPL_COURS_DE_MATIERE, TPL_PROF_DE_MATIERE, TPL_PROFESSEUR_DE_MATIERE, TPL_PROF_DE_MATIERE_FIRST_MINUS, TPL_PROFESSEUR_DE_MATIERE_FIRST_MINUS
6. `city` — TPL_IF_VILLE, TPL_VILLE, TPL_VILLE_AUTRE_NOM, TPL_VILLE_NETTOYE, TPL_VILLE_ID, TPL_VILLE_CP, TPL_VILLE_A_VILLE
7. `department` — TPL_IF_DEPARTEMENT, TPL_DEPARTEMENT, TPL_DEPARTEMENT_ID, TPL_DEPARTEMENT_URL, TPL_DEPARTEMENT_TAG_URL, TPL_DEPARTEMENT_DANS_LE_DEPARTEMENT
8. `region` — TPL_IF_REGION, TPL_REGION, TPL_REGION_ID, TPL_REGION_URL, TPL_REGION_TAG_URL
9. `level` — TPL_NIVEAU, TPL_NIVEAU_ID, TPL_NIVEAU_MINUS, TPL_NIVEAU_FIRST_MAJUS, TPL_NIVEAU_MINUS_SMART, TPL_NIVEAU_FIRST_MAJUS_SMART, TPL_NIVEAU_URL, TPL_DEMARCHE, TPL_DEMARCHE_ID
10. `landing` — TPL_NB_PROFS, TPL_NB_PROFS_FORMATE, TPL_URL_LANDING_MATIERE, TPL_URL_LANDING_MATIERE_ONLINE

From config/label_patterns.json `subject_variable_usage_rules` (lines 182–212) — these three variables already have populated `use_for` / `do_not_use_for` arrays that must be lifted verbatim into their H3 sections:
- TPL_MATIERE_DE_MATIERE
- TPL_MATIERE_FIRST_MAJUS_SMART
- TPL_MATIERE_MINUS_SMART
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the seed variables_guide.md from label_patterns.json + Variables.csv</name>
  <files>config/variables_guide.md</files>
  <read_first>
    - config/label_patterns.json (lines 49–212 — variable_categories AND subject_variable_usage_rules)
    - config/Variables.csv (816 rows — for Aide and Exemple de valeur columns)
    - .planning/phases/13-standalone-feedback-skill/13-CONTEXT.md (D-08 file structure spec)
    - .planning/phases/13-standalone-feedback-skill/13-PATTERNS.md (lines 222–278 — per-variable structure pattern)
  </read_first>
  <action>
Create `config/variables_guide.md` with this exact structure per D-08:

1. **H1 + intro** (verbatim):
   ```markdown
   # Variables Guide

   Human-readable per-variable documentation. Complements the BO-sourced `Variables.csv` catalog and the machine-readable `subject_variable_usage_rules` in `label_patterns.json`. Grows over time via the Tier 1 → Tier 2 promotion path in `/submit-feedback` (per Phase 13 D-10 — never edited by a single feedback session).

   **Categories below mirror `label_patterns.json` `variable_categories` (10 keys).**
   ```

2. **10 H2 sections** in this exact order (D-08): `general`, `category`, `subject`, `article_contractions`, `compound_phrases`, `city`, `department`, `region`, `level`, `landing`. Each H2 line is the category key surrounded by readable text. Example:
   ```markdown
   ## general

   General variables available across all templates.
   ```
   The H2 description string comes from `label_patterns.json.variable_categories.<key>.description` verbatim.

3. **H3 per variable** within each category. Pull the variable list from `label_patterns.json.variable_categories.<key>.variables`. For each variable, render this exact 4-label block:
   ```markdown
   ### `@<NAME>@`

   **Purpose:** <one sentence — seed from label_patterns.json `notes` field, then enrich from Variables.csv `Aide` column if a row exists for this variable>

   **Valid for:** <if subject_variable_usage_rules.<NAME>.use_for exists, render the comma-separated list of ISO codes; otherwise render `_(BO-configurable; see Variables.csv for default)_`>

   **Do NOT use for:** <if subject_variable_usage_rules.<NAME>.do_not_use_for exists, render the comma-separated list of ISO codes; otherwise render `_(no per-language exclusions recorded yet)_`>

   **Notes:** <seed from label_patterns.json `notes` field plus, if applicable, `subject_variable_usage_rules.<NAME>.important` and `language_notes` entries one per line>
   ```

4. **Variable name format:** wrap every variable name in backticks AND in `@...@` delimiters even if the source `name` field omits one or both (some entries in `variable_categories` use `TPL_X` without `@`). The on-disk style standard is `@TPL_NAME@`. Example: source `{"name": "TPL_MATIERE"}` → header `### \`@TPL_MATIERE@\``.

5. **Variables.csv enrichment:** For each variable, look up the row in `Variables.csv` (first column matches `@TPL_NAME@`). If the `Aide` column has content, prepend it to `**Purpose:**` followed by " — " then the `notes` text. If `Exemple de valeur` has content, append " (example: <value>)" to `**Notes:**` (truncate value to first 80 chars).

6. **Three populated variables (lift from `subject_variable_usage_rules` verbatim):** `TPL_MATIERE_DE_MATIERE`, `TPL_MATIERE_FIRST_MAJUS_SMART`, `TPL_MATIERE_MINUS_SMART` — these three have full `use_for`/`do_not_use_for`/`language_notes` blocks in `label_patterns.json` lines 184–211. Render their H3 sections with the populated arrays, the `important` text in Notes, and one bullet per `language_notes` entry. NOTE: `TPL_MATIERE_DE_MATIERE` belongs in the `compound_phrases` H2 section (per `variable_categories` line 117), and the two `_SMART` variables belong in `subject` (lines 87–88).

7. **Closing footer** (verbatim):
   ```markdown
   ---

   *Seed file generated from label_patterns.json (10 categories, ~70 variables) and Variables.csv (816-row BO catalog) — Phase 13 (FEEDBACK-11).*
   *Subsequent edits flow only through `/submit-feedback` Tier 1 → Tier 2 promotion path (per D-10).*
   ```

8. **Generation method:** Read `label_patterns.json` ONCE and iterate the JSON in memory. Read `Variables.csv` ONCE into a dict keyed by variable name. Compose the markdown in a single pass. Use the Write tool (NOT heredoc/echo) to create the file.

9. **Coverage requirement:** every variable that appears in `label_patterns.json.variable_categories.*.variables` must have an H3 entry. No omissions. Count must reach ≥ 65 H3 sections (10 categories × ~7 variables average).
  </action>
  <verify>
    <automated>test -f config/variables_guide.md &amp;&amp; head -1 config/variables_guide.md | grep -Fxq '# Variables Guide' &amp;&amp; grep -c '^## ' config/variables_guide.md | awk '$1 == 10 {exit 0} {exit 1}' &amp;&amp; [ "$(grep -c '^### ' config/variables_guide.md)" -ge 65 ] &amp;&amp; grep -q '^## general' config/variables_guide.md &amp;&amp; grep -q '^## subject' config/variables_guide.md &amp;&amp; grep -q '^## compound_phrases' config/variables_guide.md &amp;&amp; grep -q '^## landing' config/variables_guide.md &amp;&amp; grep -q '@TPL_MATIERE_DE_MATIERE@' config/variables_guide.md &amp;&amp; grep -q '@TPL_MATIERE_FIRST_MAJUS_SMART@' config/variables_guide.md &amp;&amp; grep -q '@TPL_MATIERE_MINUS_SMART@' config/variables_guide.md &amp;&amp; [ "$(grep -c '^\*\*Purpose:\*\*' config/variables_guide.md)" -ge 65 ] &amp;&amp; [ "$(grep -c '^\*\*Valid for:\*\*' config/variables_guide.md)" -ge 65 ] &amp;&amp; [ "$(grep -c '^\*\*Do NOT use for:\*\*' config/variables_guide.md)" -ge 65 ] &amp;&amp; [ "$(grep -c '^\*\*Notes:\*\*' config/variables_guide.md)" -ge 65 ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists at config/variables_guide.md
    - First line is exactly `# Variables Guide`
    - File contains exactly 10 H2 sections (one per `variable_categories` key)
    - File contains ≥ 65 H3 sections (one per variable in variable_categories)
    - All four labels (`**Purpose:**`, `**Valid for:**`, `**Do NOT use for:**`, `**Notes:**`) appear ≥ 65 times each
    - The three already-populated variables (TPL_MATIERE_DE_MATIERE, TPL_MATIERE_FIRST_MAJUS_SMART, TPL_MATIERE_MINUS_SMART) appear with the `use_for`/`do_not_use_for` arrays from label_patterns.json embedded verbatim
    - `wc -l config/variables_guide.md` ≥ 300 lines
  </acceptance_criteria>
  <done>variables_guide.md exists with 10 H2 sections, ≥65 H3 entries, and the three populated subject variables render the use_for/do_not_use_for arrays verbatim from label_patterns.json.</done>
</task>

<task type="auto">
  <name>Task 2: Add FEEDBACK-11 to REQUIREMENTS.md and update ROADMAP.md Phase 13 line</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — both the FEEDBACK section list AND the Traceability table at the bottom)
    - .planning/ROADMAP.md (Phase 13 entry around line 54–67)
    - .planning/phases/13-standalone-feedback-skill/13-CONTEXT.md (D-07 — the verbatim FEEDBACK-11 wording)
  </read_first>
  <action>
**Edit 1 — REQUIREMENTS.md FEEDBACK section.** After the existing `FEEDBACK-10` line (currently the last entry in the §FEEDBACK list), append this new line verbatim:

```markdown
- [ ] **FEEDBACK-11**: `config/variables_guide.md` exists as a populated human-readable per-variable documentation file (seeded from `Variables.csv` + `label_patterns.json.variable_categories`); it is the Tier 2 destination for variable-related rule promotions written by `/submit-feedback`; backup-on-first-write applies (`corrections/backups/YYYYMMDDTHHMMSSZ_variables_guide.md`)
```

**Edit 2 — REQUIREMENTS.md Traceability table.** Add a new row after the `FEEDBACK-10` row in the table:

```markdown
| FEEDBACK-11 | Phase 13 | Pending |
```

**Edit 3 — REQUIREMENTS.md Coverage block.** Update the coverage summary at the bottom from:
```
- v1.3 requirements: 24 total
- Mapped to phases: 24 (Phase 13: 10, Phase 14: 7, Phase 15: 7)
```
to:
```
- v1.3 requirements: 25 total
- Mapped to phases: 25 (Phase 13: 11, Phase 14: 7, Phase 15: 7)
```

**Edit 4 — ROADMAP.md Phase 13 Requirements line.** Find the line:
```
**Requirements**: FEEDBACK-01, FEEDBACK-02, FEEDBACK-03, FEEDBACK-04, FEEDBACK-05, FEEDBACK-06, FEEDBACK-07, FEEDBACK-08, FEEDBACK-09, FEEDBACK-10
```
Replace with:
```
**Requirements**: FEEDBACK-01, FEEDBACK-02, FEEDBACK-03, FEEDBACK-04, FEEDBACK-05, FEEDBACK-06, FEEDBACK-07, FEEDBACK-08, FEEDBACK-09, FEEDBACK-10, FEEDBACK-11
```

NOTE: The PARALLEL-06 → FEEDBACK-12 move and ROADMAP Phase 15 update are owned by Plan 13-06 (Step 7 deletion plan). This task only handles FEEDBACK-11.

Use the Edit tool — do NOT rewrite the whole file.
  </action>
  <verify>
    <automated>grep -F '**FEEDBACK-11**' .planning/REQUIREMENTS.md &amp;&amp; grep -F '| FEEDBACK-11 | Phase 13 | Pending |' .planning/REQUIREMENTS.md &amp;&amp; grep -F 'v1.3 requirements: 25 total' .planning/REQUIREMENTS.md &amp;&amp; grep -F 'Mapped to phases: 25 (Phase 13: 11' .planning/REQUIREMENTS.md &amp;&amp; grep -F 'FEEDBACK-10, FEEDBACK-11' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - REQUIREMENTS.md contains the literal string `**FEEDBACK-11**`
    - REQUIREMENTS.md Traceability table contains row `| FEEDBACK-11 | Phase 13 | Pending |`
    - REQUIREMENTS.md Coverage block reads `v1.3 requirements: 25 total` and `Mapped to phases: 25 (Phase 13: 11, Phase 14: 7, Phase 15: 7)`
    - ROADMAP.md Phase 13 Requirements line ends with `..., FEEDBACK-10, FEEDBACK-11`
  </acceptance_criteria>
  <done>FEEDBACK-11 is registered in REQUIREMENTS.md (list + traceability + coverage) and surfaced in ROADMAP.md Phase 13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → variables_guide.md | Static documentation file, no input parsing at runtime; written once by a planner/Claude session, then read-only until promotion-path writes |
| disk → REQUIREMENTS.md / ROADMAP.md | Project-internal planning files; edited via Edit tool; no external input |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13.01-01 | Tampering | config/variables_guide.md write | mitigate | Use Write tool (atomic via OS rename) — no partial-write window. File is plain markdown, no executable content. |
| T-13.01-02 | Tampering | label_patterns.json source read | accept | Read-only access during seed generation; no write-back. If source is malformed JSON the read fails fast and the seed task aborts before writing variables_guide.md. |
| T-13.01-03 | Information Disclosure | Variables.csv example values copied into Notes | accept | All Variables.csv content is already committed to this repo and contains no PII (URLs, prices, generic example strings). |
| T-13.01-04 | Denial of Service | 816-row CSV × 70-variable join | accept | One-pass dict lookup, O(n+m); completes in <1s. No regex backtracking risk. |
| T-13.01-05 | Repudiation | REQUIREMENTS.md / ROADMAP.md edits | accept | Edits committed to git per phase commit step; full audit trail. |
</threat_model>

<verification>
Run the automated verify commands in each task. Then visually scan `config/variables_guide.md` to confirm the three populated subject variables (`TPL_MATIERE_DE_MATIERE`, `TPL_MATIERE_FIRST_MAJUS_SMART`, `TPL_MATIERE_MINUS_SMART`) carry the full `use_for`/`do_not_use_for`/`language_notes` content from `label_patterns.json` — these three must look populated, not placeholder.
</verification>

<success_criteria>
- `config/variables_guide.md` exists with 10 H2 sections + ≥65 H3 entries + all 4 labels per H3
- The three subject variables with existing `subject_variable_usage_rules` data render their `use_for`/`do_not_use_for` arrays verbatim
- `.planning/REQUIREMENTS.md` registers FEEDBACK-11 in the FEEDBACK list, the traceability table, and the coverage block
- `.planning/ROADMAP.md` Phase 13 Requirements line includes FEEDBACK-11
</success_criteria>

<output>
After completion, create `.planning/phases/13-standalone-feedback-skill/13-01-SUMMARY.md` documenting:
- Number of H3 entries generated per category
- The three pre-populated variables and where their data came from
- Total file size and category coverage map
- Confirmation that the file is now ready as the Tier 2 destination for `/submit-feedback` (Plan 13-05 promotion phase)
</output>
