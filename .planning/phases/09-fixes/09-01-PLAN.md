---
phase: 09-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/structural_validator.py
autonomous: true
requirements: [FIX-03, FIX-04]

must_haves:
  truths:
    - "Swapping the France row to any position in the CSV does not break the review run"
    - "Adding a brand-new emoji to a translation is flagged without any code changes"
  artifacts:
    - path: "scripts/structural_validator.py"
      provides: "France row search by content + Unicode-aware emoji detection"
      contains: "unicodedata"
  key_links:
    - from: "scripts/structural_validator.py"
      to: "run_validation()"
      via: "France row lookup by country name or language code"
      pattern: "france|fr"
    - from: "scripts/structural_validator.py"
      to: "check_emojis()"
      via: "unicodedata.category() emoji detection"
      pattern: "unicodedata\\.category"
---

<objective>
Fix the two structural validator brittleness issues: France reference row assumption and hardcoded emoji ranges.

Purpose: The structural validator silently produces wrong results when the France row is not at position 0, and misses emoji from Unicode versions newer than the hardcoded ranges. These are the two highest-priority code fixes from the audit.

Output: Updated `scripts/structural_validator.py` with robust France row detection and Unicode-data-based emoji detection.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-project-audit/08-01-raw-findings.md
@.planning/phases/08-project-audit/08-02-gap-findings.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix France reference row detection in run_validation()</name>
  <files>scripts/structural_validator.py</files>
  <read_first>
    - scripts/structural_validator.py (lines 580-620 — current run_validation function with hardcoded entries[0])
  </read_first>
  <behavior>
    - Test 1: CSV with France as first row works identically to current behavior
    - Test 2: CSV with France as third row (e.g., Germany, Spain, France, Italy) finds France and validates other rows against it
    - Test 3: CSV with no France row at all exits with a clear error message containing "France" and "reference"
    - Test 4: Case-insensitive match — "france", "France", "FRANCE" all work
    - Test 5: Match on country field containing "France" OR language code "fr" / "FR"
  </behavior>
  <action>
    In `run_validation()` (around line 596-598), replace:
    ```python
    # First entry should be France (the reference)
    ref_entry = entries[0]
    other_entries = entries[1:]
    ```

    With a search function that:
    1. Iterates all entries looking for one where `entry['country']` matches "France" (case-insensitive) OR where a language/locale field contains "fr" or "FR" as a standalone code (not as a substring of e.g. "Africa")
    2. The match logic: `entry['country'].strip().lower() == 'france'` OR the column header for that entry's language is exactly `fr` or `FR` (check `entry.get('language', '').strip().lower() == 'fr'`)
    3. If found, set `ref_entry` to that entry and `other_entries` to all other entries (preserving order)
    4. If NOT found, print to stderr: `ERROR: No France/fr reference row found in CSV. The structural validator requires a French reference row to compare against.` and call `sys.exit(1)`
    5. Also check what field `parse_per_notification_csv()` populates — look at the parse function to see what dict keys each entry has (likely 'country', 'titre', 'corps'). The France detection should use whatever field contains the country/language identifier.

    Read `parse_per_notification_csv()` to confirm the exact dict keys. The entry dict likely has a 'country' key from the column header. Match on that key containing "France" or being "fr"/"FR".
  </action>
  <verify>
    <automated>python3 -c "
import sys; sys.path.insert(0, 'scripts')
from structural_validator import run_validation
# This should not crash — validates the function exists and is callable
print('France row detection function exists')
"</automated>
  </verify>
  <acceptance_criteria>
    - structural_validator.py contains `france` or `France` string comparison logic in run_validation()
    - structural_validator.py does NOT contain the line `ref_entry = entries[0]` (the hardcoded assumption is removed)
    - structural_validator.py contains `sys.exit(1)` with an error message mentioning "France" or "reference" when no France row is found
    - The function handles entries list where France is at any position (not just index 0)
  </acceptance_criteria>
  <done>France reference row is found by searching entry content (country name or language code), not by assuming position 0. Missing France row produces a clear error instead of silently using the wrong reference.</done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded emoji ranges with unicodedata-based detection</name>
  <files>scripts/structural_validator.py</files>
  <read_first>
    - scripts/structural_validator.py (lines 54-70 — current RE_EMOJI definition, lines 280-330 — check_emojis function)
  </read_first>
  <action>
    Replace the hardcoded `RE_EMOJI` regex (lines 55-70) with a function that uses Python's built-in `unicodedata` module (stdlib, no pip install needed).

    1. Add `import unicodedata` at the top of the file (near the existing imports around line 1-10).

    2. Create a new function `is_emoji_char(char: str) -> bool` that returns True if `unicodedata.category(char)` starts with `'So'` (Symbol, other — covers most emoji) or if the character is in the known emoji-adjacent categories. The precise logic:
       ```python
       def is_emoji_char(char: str) -> bool:
           """Detect emoji using Unicode category data (auto-updates with Python version)."""
           if len(char) != 1:
               return False
           cat = unicodedata.category(char)
           # Symbol, Other covers the vast majority of emoji
           if cat == 'So':
               return True
           # Also catch specific codepoint ranges that unicodedata classifies differently
           cp = ord(char)
           # Regional indicator symbols (flags) — category 'So' on most Python versions
           if 0x1F1E0 <= cp <= 0x1F1FF:
               return True
           # Variation selectors and ZWJ used in emoji sequences
           if cp in (0x200D, 0xFE0F, 0xFE0E):
               return True
           return False
       ```

    3. Create a replacement function `extract_emoji(text: str) -> list[str]` that returns a list of emoji characters found in the text:
       ```python
       def extract_emoji(text: str) -> list[str]:
           """Extract all emoji characters from text using unicodedata."""
           return [ch for ch in text if is_emoji_char(ch)]
       ```

    4. Update `check_emojis()` to use `extract_emoji()` instead of `RE_EMOJI.findall()`. Find the lines in check_emojis where `RE_EMOJI` is used (likely `ref_emoji = set(RE_EMOJI.findall(ref_text))` and similar) and replace with `ref_emoji = set(extract_emoji(ref_text))`.

    5. Remove the `RE_EMOJI` compiled regex constant entirely (lines 55-70). Keep all other RE_* constants.

    6. Do NOT add any pip dependencies. `unicodedata` is stdlib.
  </action>
  <verify>
    <automated>python3 -c "
import sys; sys.path.insert(0, 'scripts')
from structural_validator import is_emoji_char, extract_emoji
# Test common emoji
assert is_emoji_char('\U0001F600'), 'Grinning face should be emoji'
assert is_emoji_char('\U0001F4A1'), 'Light bulb should be emoji'
# Test non-emoji
assert not is_emoji_char('A'), 'Letter A should not be emoji'
assert not is_emoji_char('5'), 'Digit 5 should not be emoji'
# Test extract
result = extract_emoji('Hello \U0001F600 world \U0001F4A1')
assert len(result) == 2, f'Expected 2 emoji, got {len(result)}'
print('All emoji detection tests passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - structural_validator.py contains `import unicodedata`
    - structural_validator.py contains `def is_emoji_char(`
    - structural_validator.py contains `def extract_emoji(`
    - structural_validator.py does NOT contain the old `RE_EMOJI = re.compile(` block
    - check_emojis() function uses extract_emoji() instead of RE_EMOJI.findall()
  </acceptance_criteria>
  <done>Emoji detection uses unicodedata categories (auto-updates with Python version) instead of hardcoded Unicode ranges. New emoji from future Unicode versions are caught without code changes.</done>
</task>

</tasks>

<verification>
1. `python3 scripts/structural_validator.py samples/*.csv` runs without error (if a sample CSV exists)
2. `grep -c "RE_EMOJI" scripts/structural_validator.py` returns 0 (old regex removed)
3. `grep -c "unicodedata" scripts/structural_validator.py` returns at least 1
4. `grep -c "entries\[0\]" scripts/structural_validator.py` returns 0 (hardcoded index removed)
</verification>

<success_criteria>
- France row is located by content search, not position assumption
- Missing France row produces a clear error exit
- Emoji detection uses unicodedata (stdlib), not hardcoded ranges
- Both changes are backward-compatible with existing CSV formats
</success_criteria>

<output>
After completion, create `.planning/phases/09-fixes/09-01-SUMMARY.md`
</output>
