---
phase: 05-notion-publishing
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .claude/commands/review-translations.md
autonomous: true
requirements:
  - NTIO-01
  - NTIO-02
  - NTIO-03
must_haves:
  truths:
    - "After a review run completes, a Notion page exists under the Reports parent page without the user running any extra command"
    - "The Notion page contains all the same sections as the .md report — summary table, French reference, grouped/single-market sections, clean markets, undefined variables"
    - "The Notion page title contains both the notification ID and the review date"
    - "If Notion publish fails, the session continues to Step 7 with a warning — it does not abort"
  artifacts:
    - path: ".claude/commands/review-translations.md"
      provides: "Notion publish block in Step 6, content adaptation instructions"
      contains: "mcp__claude_ai_Notion__notion-create-pages"
  key_links:
    - from: "Step 6 Notion publish block"
      to: "mcp__claude_ai_Notion__notion-create-pages"
      via: "MCP tool call with title, parent_page_id, page_content"
      pattern: "notion-create-pages"
    - from: "Step 1 sanitized notification ID"
      to: "Step 6 Notion page title"
      via: "Title format: Translation Review — [id] — [date]"
      pattern: "Translation Review"
    - from: "Step 6 .md content"
      to: "Step 6 Notion page_content"
      via: "Content adaptation: table conversion + template variable escaping"
      pattern: "table.*HTML"
---

<objective>
Add automatic Notion publishing to the review-translations skill so that every completed review creates a Notion page under the Reports hub.

Purpose: Team members can access review reports directly in Notion without manual copy-paste or file sharing. The Notion page mirrors the .md report content with proper formatting. This is the core deliverable of Phase 5.

Output: Updated `.claude/commands/review-translations.md` with a Notion publish block in Step 6 and updated output announcements.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-notion-publishing/05-CONTEXT.md
@.planning/phases/05-notion-publishing/05-RESEARCH.md
@.planning/phases/05-notion-publishing/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Notion content adaptation instructions to Step 6</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (the FULL file — you are modifying it; Plan 01 has already removed HTML)
    - .planning/phases/05-notion-publishing/05-RESEARCH.md (Notion-flavored Markdown format, table conversion, template variable escaping)
    - .planning/phases/05-notion-publishing/05-CONTEXT.md (decisions D-01 through D-12)
  </read_first>
  <action>
Insert a new section in Step 6, AFTER the .md file is written to disk and AFTER the MD-to-HTML conversion step (which now only runs for pdf), but BEFORE the output announcement line. This new section has two parts: content adaptation and the Notion publish call.

**Part A — Notion content adaptation instructions.**

Insert this new subsection:

```markdown
### Notion publish

After writing the .md report to disk, prepare and publish the report to Notion.

**Step 6a — Construct the page title:**

```
Translation Review — [notification_id] — [YYYY-MM-DD]
```

Where `[notification_id]` is the sanitized ID from Step 1 (e.g. `relance_3`) and `[YYYY-MM-DD]` is today's date.

**Step 6b — Adapt .md content for Notion-flavored Markdown:**

Take the .md report content that was just written to disk and apply these transformations to produce the `notion_content` string:

1. **Table conversion**: Convert ALL Markdown pipe-syntax tables to HTML `<table>` format. This includes:
   - The Summary table (Section 1)
   - The Undefined variables table (Section 6, if present)
   
   Conversion rule: For each pipe-syntax table, produce:
   ```html
   <table>
   <tr><th>Header1</th><th>Header2</th>...</tr>
   <tr><td>Cell1</td><td>Cell2</td>...</tr>
   ...
   </table>
   ```
   Remove the pipe-syntax separator row (`|---|---|`). Each `|`-delimited data row becomes a `<tr>` with `<td>` cells. The header row uses `<th>` cells.

2. **Template variable escaping**: Ensure every `@TPL_*@` variable in the report is wrapped in backtick code spans (`` `@TPL_VARIABLE_NAME@` ``). Most will already be in code spans or code blocks from the .md report. Scan for any bare `@TPL_` occurrences NOT already inside backticks or fenced code blocks and wrap them.

3. **HTML-like template tags**: Ensure square-bracket tags like `[TITRE]`, `[/TITRE]`, `[LIEN]`, `[/LIEN]`, `[BOUTON]`, `[/BOUTON]` in translation body text are wrapped in backtick code spans to prevent Notion from misinterpreting them.

4. **Translation body blocks**: When "Current text" or "Proposed text" sections contain full translation bodies with template tags and variables, wrap the entire body value in a fenced code block (triple backticks) rather than inline code spans, since bodies can be multi-line.

**Step 6c — Publish to Notion:**

Call `mcp__claude_ai_Notion__notion-create-pages` with:
- `title`: the page title from Step 6a
- `parent_page_id`: `33dd6418695a8097998fcf373ed18bf5`
- The adapted markdown content from Step 6b as the page body content parameter

**Parameter name uncertainty**: The body content parameter may be named `page_content`, `content`, or `markdown`. Try the call with the content string. If the tool returns an error suggesting a different parameter name, retry with the alternate name. The soft-fail behavior (Step 6d) handles persistent failures gracefully.

**Step 6d — Handle result:**

- **On success**: Capture the returned page URL from the tool response. If the response contains a page `id` but no direct URL, construct the URL as `https://www.notion.so/[page_id_without_hyphens]`. Store the URL for the output announcement.
- **On failure**: Capture the error message. Do NOT abort the session. Store the error for the output announcement. The review is still complete — the .md file exists on disk.
```

**Part B — Update the output announcement.**

Replace the existing output announcement at the end of Step 6. The current text (after Plan 01) says something like "Tell the user about the generated files per the output behavior described above (md default: .md only; pdf: .md + .pdf or fallback message)."

Replace that final announcement line with:

```markdown
**Output announcement:**

After the Notion publish attempt, announce results to the user:

- **Notion success + md format (default):**
  "Report generated at `reports/review-[id]-[date].md`
  Published to Notion: [page URL]"

- **Notion success + pdf format:**
  "Report generated at `reports/review-[id]-[date].pdf` (Markdown source: `.md`)
  Published to Notion: [page URL]"

- **Notion failure (any format):**
  "Report generated at `reports/review-[id]-[date].md`
  Warning: Notion publish failed: [error message]. Report saved locally."

After the announcement, proceed to Step 7 regardless of Notion success or failure.
```

This implements:
- D-01: parent page ID `33dd6418695a8097998fcf373ed18bf5`
- D-02: new sub-page per run (create-pages, not update)
- D-03: publish after report written, before Step 7
- D-04: .md written first, then Notion publish
- D-05: title format `Translation Review — [id] — [YYYY-MM-DD]`
- D-06: native Notion blocks via Notion-flavored Markdown (table HTML, code spans for variables)
- D-09: output announces .md path + Notion URL on success
- D-10/D-11/D-12: soft-fail with warning, continue to Step 7
  </action>
  <verify>
    <automated>grep -c "mcp__claude_ai_Notion__notion-create-pages" ".claude/commands/review-translations.md" | grep -q "^1$" && grep -c "33dd6418695a8097998fcf373ed18bf5" ".claude/commands/review-translations.md" | grep -q "^1$" && grep -c "Translation Review —" ".claude/commands/review-translations.md" | grep -q "^[1-9]" && grep -c "Notion publish failed" ".claude/commands/review-translations.md" | grep -q "^1$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `mcp__claude_ai_Notion__notion-create-pages` returns exactly 1 match in the file
    - grep for `33dd6418695a8097998fcf373ed18bf5` returns exactly 1 match (the parent_page_id)
    - grep for `Translation Review —` returns at least 1 match (the title format)
    - grep for `Notion publish failed` returns exactly 1 match (the failure warning template)
    - grep for `Published to Notion:` returns at least 1 match (the success announcement)
    - The Notion publish section appears AFTER the .md write and AFTER the MD-to-HTML conversion step
    - The Notion publish section appears BEFORE Step 7
    - grep for `<table>` appears in the content adaptation instructions (table conversion rule)
    - grep for backtick-wrapped `@TPL_` pattern appears in the template variable escaping instructions
    - The output announcement has three cases: Notion success + md, Notion success + pdf, Notion failure
    - The phrase "Do NOT abort" or equivalent appears in the failure handling instructions
  </acceptance_criteria>
  <done>Step 6 contains a complete Notion publish block: content adaptation (table conversion, variable escaping), MCP tool call with title/parent_page_id/content, success/failure handling, and updated output announcements covering all three cases. The block is positioned after .md write and before Step 7.</done>
</task>

</tasks>

<verification>
1. Read the full `.claude/commands/review-translations.md` file
2. Confirm the Notion publish block exists between the .md write and Step 7
3. Confirm parent_page_id is `33dd6418695a8097998fcf373ed18bf5`
4. Confirm title format is `Translation Review — [notification_id] — [YYYY-MM-DD]`
5. Confirm table conversion instructions specify HTML `<table>` format
6. Confirm template variable escaping instructions mention backtick code spans for `@TPL_*@`
7. Confirm failure handling is soft-warn (does not abort, continues to Step 7)
8. Confirm output announcement covers success and failure cases
9. Confirm no references to HTML as a user-facing format remain (from Plan 01)
</verification>

<success_criteria>
- The MCP tool call `mcp__claude_ai_Notion__notion-create-pages` appears in the skill with correct parent_page_id
- Page title includes notification ID and date
- Content adaptation covers: table conversion to HTML, template variable code spans, translation body fenced code blocks
- Failure is soft-warn, session continues to Step 7
- Output announcement shows Notion URL on success, warning on failure
</success_criteria>

<output>
After completion, create `.planning/phases/05-notion-publishing/05-02-SUMMARY.md`
</output>
