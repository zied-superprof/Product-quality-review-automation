---
phase: 05-notion-publishing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/review-translations.md
autonomous: true
requirements:
  - NTIO-04
must_haves:
  truths:
    - "Running /review-translations with no --format flag does NOT produce an .html file"
    - "Running /review-translations --format html prints an error about invalid format"
    - "Running /review-translations --format pdf still works (HTML intermediate is internal)"
  artifacts:
    - path: ".claude/commands/review-translations.md"
      provides: "Updated skill with HTML format removed"
      contains: "--format md|pdf"
  key_links:
    - from: "Step 0 --format flag"
      to: "Step 6 output behavior"
      via: "format variable controls which output paths execute"
      pattern: "--format md\\|pdf"
---

<objective>
Remove HTML as a report output format from the review-translations skill.

Purpose: HTML output is being replaced by Notion publishing (Plan 02). The HTML format option, its default status, the output behavior description, and the MD-to-HTML conversion trigger for the `html` path must all be removed. The `pdf` path retains the HTML intermediate file internally per D-08.

Output: Updated `.claude/commands/review-translations.md` with HTML format removed from all three locations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-notion-publishing/05-CONTEXT.md
@.planning/phases/05-notion-publishing/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove HTML format option from Step 0 and Step 6</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (the FULL file — you are modifying it)
    - .planning/phases/05-notion-publishing/05-CONTEXT.md (decisions D-07, D-08, D-09)
  </read_first>
  <action>
Three locations in `.claude/commands/review-translations.md` must be updated. Make ALL three changes:

**Location 1 — Step 0, line 16 (the --format flag definition):**

Replace:
```
- `--format html|md|pdf` — optional, controls report output. Default: `html` (writes both .md and .html). `md` writes .md only. `pdf` writes .md + .html + .pdf (requires weasyprint). If an unrecognized value is passed, print: `Unknown format "[value]". Valid options: html, md, pdf` and abort.
```

With:
```
- `--format md|pdf` — optional, controls report output. Default: `md` (writes .md only). `pdf` writes .md + .pdf (requires weasyprint). If an unrecognized value is passed, print: `Unknown format "[value]". Valid options: md, pdf` and abort.
```

**Location 2 — Step 6, lines 165-168 (output behavior by --format flag):**

Replace the entire output behavior block:
```
**Output behavior by --format flag:**
- `html` (default): Write the Markdown report to `reports/review-[id]-[date].md`, then convert to HTML and write to `reports/review-[id]-[date].html`. Tell the user: "Report generated at `reports/review-[id]-[date].html` (Markdown source: `.md`)"
- `md`: Write only the Markdown report to `reports/review-[id]-[date].md`. Tell the user: "Report generated at `reports/review-[id]-[date].md`"
- `pdf`: Write .md, convert to .html, then convert to .pdf using weasyprint. If weasyprint is not installed, print: "PDF generation requires weasyprint. Install with: pip install weasyprint" and fall back to html behavior. Tell the user about all generated files.
```

With:
```
**Output behavior by --format flag:**
- `md` (default): Write the Markdown report to `reports/review-[id]-[date].md`. Tell the user: "Report generated at `reports/review-[id]-[date].md`"
- `pdf`: Write .md, convert to .html internally (not announced), then convert to .pdf using weasyprint. If weasyprint is not installed, print: "PDF generation requires weasyprint. Install with: pip install weasyprint" and fall back to md behavior. Tell the user: "Report generated at `reports/review-[id]-[date].pdf` (Markdown source: `.md`)"
```

**Location 3 — Step 6, line 170 (MD-to-HTML conversion step trigger):**

Replace:
```
**MD-to-HTML conversion step** (runs for `html` and `pdf` formats):
```

With:
```
**MD-to-HTML conversion step** (runs only for `pdf` format — HTML is an internal intermediate, not announced as output):
```

**Location 4 — Step 6, line 161 (filename description):**

Replace:
```
**Filename**: `reports/review-[notification-id]-YYYY-MM-DD.md` (and `.html` / `.pdf` as applicable)
```

With:
```
**Filename**: `reports/review-[notification-id]-YYYY-MM-DD.md` (and `.pdf` if `--format pdf`)
```

**Location 5 — Step 6, line 321 (end-of-Step-6 announcement reminder):**

Replace:
```
Tell the user about the generated files per the output behavior described above (html default: both .md and .html; md: .md only; pdf: .md + .html + .pdf or fallback message).
```

With:
```
Tell the user about the generated files per the output behavior described above (md default: .md only; pdf: .md + .pdf or fallback message).
```

Per D-07: HTML is no longer a valid option. Per D-08: the pdf path still generates .html as intermediate but does not announce it. Per D-09: the output announcement will be further updated in Plan 02 to include the Notion URL.
  </action>
  <verify>
    <automated>grep -c "html|md|pdf" ".claude/commands/review-translations.md" | grep "^0$" && grep -c "--format md|pdf" ".claude/commands/review-translations.md" | grep "^1$" && echo "PASS" || echo "Check manually"</automated>
  </verify>
  <acceptance_criteria>
    - grep for `--format html|md|pdf` returns 0 matches in the file
    - grep for `--format md|pdf` returns exactly 1 match (in Step 0)
    - grep for `Default: \`html\`` returns 0 matches
    - grep for `Default: \`md\`` returns at least 1 match
    - grep for `html (default)` returns 0 matches in the output behavior section
    - grep for `runs for \`html\` and \`pdf\` formats` returns 0 matches
    - grep for `runs only for \`pdf\` format` returns exactly 1 match
    - The string `Valid options: html, md, pdf` does NOT appear anywhere in the file
    - The string `Valid options: md, pdf` appears exactly once
    - The pdf output behavior bullet still mentions weasyprint
  </acceptance_criteria>
  <done>The --format flag accepts only md and pdf. Default is md. HTML is not a valid option. The MD-to-HTML conversion block only triggers for pdf. All five locations updated consistently.</done>
</task>

</tasks>

<verification>
1. Read the full `.claude/commands/review-translations.md` file
2. Confirm no remaining references to `html` as a user-facing format option (the word "html" may still appear in the Python code block for the pdf intermediate and in weasyprint instructions — that is correct)
3. Confirm `--format md|pdf` with default `md` in Step 0
4. Confirm the output behavior section has exactly 2 bullets (md and pdf)
5. Confirm the MD-to-HTML conversion step says "runs only for `pdf` format"
</verification>

<success_criteria>
- The string `--format html` does not appear as a valid option anywhere in the skill
- Default format is `md`, not `html`
- The pdf path still works (HTML intermediate is kept but not announced)
- All five edit locations are consistent with each other
</success_criteria>

<output>
After completion, create `.planning/phases/05-notion-publishing/05-01-SUMMARY.md`
</output>
