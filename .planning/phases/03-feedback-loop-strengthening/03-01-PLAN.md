---
phase: 03-feedback-loop-strengthening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - corrections/corrections_log.json
  - .claude/commands/review-translations.md
autonomous: true
requirements:
  - FBK-01
  - FBK-02

must_haves:
  truths:
    - "corrections_log.json has a _schema block with exactly 8 fields matching D-02"
    - "corrections_log.json no longer contains a rules_summary array"
    - "Step 7 in review-translations.md writes one structured entry per market per feedback item with all 8 fields"
    - "Step 7 performs pre-write conflict detection against skill file and config files before writing"
  artifacts:
    - path: "corrections/corrections_log.json"
      provides: "Structured corrections store with new 8-field schema"
      contains: "_schema"
    - path: ".claude/commands/review-translations.md"
      provides: "Rewritten Step 7 with structured writes and conflict detection"
      contains: "issue_category"
  key_links:
    - from: ".claude/commands/review-translations.md"
      to: "corrections/corrections_log.json"
      via: "Step 7 structured write"
      pattern: "confidence.*high.*medium.*low"
    - from: ".claude/commands/review-translations.md"
      to: "config/label_patterns.json"
      via: "Pre-write conflict check"
      pattern: "Conflict detected"
---

<objective>
Define the new 8-field corrections schema in corrections_log.json and rewrite Step 7 of review-translations.md to write structured per-market entries with pre-write conflict detection.

Purpose: FBK-01 makes the corrections log machine-readable for future generation tooling. FBK-02 replaces the freeform feedback write with structured records and adds safety via conflict detection.
Output: Updated corrections_log.json (new schema, no rules_summary array), rewritten Step 7 in review-translations.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feedback-loop-strengthening/03-CONTEXT.md
@.planning/phases/03-feedback-loop-strengthening/03-RESEARCH.md

@corrections/corrections_log.json
@.claude/commands/review-translations.md
@config/label_patterns.json
@config/tone_guidelines.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace corrections_log.json schema and remove rules_summary array (FBK-01, D-01 through D-06, D-09)</name>
  <files>corrections/corrections_log.json</files>
  <read_first>
    - corrections/corrections_log.json (current file — confirm empty arrays, old _schema)
    - .planning/phases/03-feedback-loop-strengthening/03-CONTEXT.md (D-01 through D-06 locked decisions)
    - .planning/phases/03-feedback-loop-strengthening/03-RESEARCH.md (Pattern 1: 8-Field Corrections Entry Schema)
  </read_first>
  <action>
Overwrite corrections/corrections_log.json with the following exact JSON structure:

```json
{
  "corrections": [],
  "_schema": {
    "correction": {
      "language": "ISO language code (string, single value — e.g. 'lt', 'ar', 'es_AR')",
      "notification_type": "BO notification ID (e.g. 'reco-val-prof-bo', 'relance_3')",
      "issue_category": "grammar | tone | label | cultural | emoji | encoding | format",
      "original": "The flagged text or finding description before correction",
      "corrected": "The corrected text or dismissal reason (for false-positive corrections per D-06)",
      "rule_extracted": "Generalized rule for future reviews",
      "confidence": "high | medium | low",
      "date": "ISO 8601 date (YYYY-MM-DD)"
    }
  }
}
```

Key changes from the old file (per D-01, D-02, D-04, D-09):
- Remove the `rules_summary` array entirely (D-09 — rules_summary.json becomes the single source of truth in Plan 03-02)
- Remove the old `_schema.rule` block (no longer embedded)
- Replace `_schema.correction` with the 8-field spec above
- Old field renames: `category` -> `issue_category`, `notification_id` -> `notification_type`, `original_text` -> `original`, `corrected_text` -> `corrected`, `extracted_rule` -> `rule_extracted`
- Removed fields: `country`, `description`
- Added field: `confidence`
- `corrections` remains `[]` (the file is currently empty — no row-by-row migration needed)
  </action>
  <verify>
    <automated>cd "/Users/juan/Desktop/Superprof Projects/Product quality review automation" && python3 -c "
import json
with open('corrections/corrections_log.json') as f:
    data = json.load(f)
schema_fields = set(data['_schema']['correction'].keys())
required = {'language','notification_type','issue_category','original','corrected','rule_extracted','confidence','date'}
assert schema_fields == required, f'Schema mismatch: {schema_fields} != {required}'
assert 'rules_summary' not in data, 'rules_summary array still present'
assert 'rule' not in data.get('_schema', {}), 'Old rule schema still present'
assert isinstance(data['corrections'], list), 'corrections is not a list'
print('PASS: 8-field schema, no rules_summary, no old rule schema')
"</automated>
  </verify>
  <acceptance_criteria>
    - corrections_log.json parses as valid JSON
    - corrections_log.json contains key "_schema" with nested "correction" having exactly 8 keys: language, notification_type, issue_category, original, corrected, rule_extracted, confidence, date
    - corrections_log.json does NOT contain key "rules_summary"
    - corrections_log.json does NOT contain "_schema.rule" (old embedded rule schema)
    - corrections_log.json contains "corrections" as an empty array
  </acceptance_criteria>
  <done>corrections_log.json has the new 8-field schema, rules_summary array removed, no old schema remnants</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Step 7 in review-translations.md for structured writes with conflict detection (FBK-02, D-07, D-08, D-17 through D-20)</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (current file — find Step 7 section starting at "## Step 7: Feedback loop")
    - .planning/phases/03-feedback-loop-strengthening/03-CONTEXT.md (D-07, D-08, D-17 through D-20)
    - .planning/phases/03-feedback-loop-strengthening/03-RESEARCH.md (Pattern 4: Pre-Write Conflict Detection, Step 7 Replacement Logic)
    - corrections/corrections_log.json (the file Step 7 writes to — confirm new schema from Task 1)
  </read_first>
  <action>
Replace the entire Step 7 section in review-translations.md (from "## Step 7: Feedback loop" to end of file) with the following. Preserve the Step 7 header and the feedback prompt + numbered index listing exactly as they are now (lines 302-325 in current file). Replace only the "When the user replies with numbers + notes:" block (lines 327-332) with this new logic:

```markdown
When the user replies with numbers + notes, process each feedback item:

### 7a — Pre-write conflict check (per D-17 through D-20)

Before writing any correction or rule, read these files and check for contradictions:
- `.claude/commands/review-translations.md` — Steps 4c and 3 sections
- `config/label_patterns.json` — if the rule touches variable usage (check `subject_variable_usage_rules`)
- `config/tone_guidelines.json` — if the rule touches formality (check `formality_rules`)

**What counts as a conflict:**
- New rule says "do NOT flag X for language Y" but skill Step 4c says "always flag X" (or vice versa)
- New rule says "use variable A for language Z" but label_patterns.json `subject_variable_usage_rules` says use variable B
- New rule contradicts a formality classification in tone_guidelines.json

**What is NOT a conflict:**
- New rule adds guidance for a case not covered by existing rules (additive rule)
- New rule refines an existing rule without contradicting it

**If conflict detected** — do NOT write. Show:
```
Conflict detected before writing:

New rule:       "[extracted rule text]"
Conflicts with: [filename] [section] — "[existing rule or behavior text]"

Which takes precedence?
1. Write the new rule anyway
2. Discard the new rule
3. Update the existing process instead
```
Wait for user choice before proceeding.

**If no conflict** — proceed silently to write (no confirmation prompt per D-20).

### 7b — Write structured correction entry (per D-07, D-08)

For each feedback item, write ONE entry per market to `corrections/corrections_log.json` > `corrections` array. If a feedback item applies to 5 markets, that is 5 separate entries — each with a single `language` string value (per D-07).

Each entry has exactly these 8 fields:
```json
{
  "language": "[market ISO code — string, not array, e.g. 'ar', 'lt', 'es_AR']",
  "notification_type": "[BO notification ID from this review session, e.g. 'relance_3']",
  "issue_category": "[one of: grammar | tone | label | cultural | emoji | encoding | format]",
  "original": "[the flagged finding text from the report]",
  "corrected": "[the corrected text, OR dismissal reason if false-positive per D-06]",
  "rule_extracted": "[generalized rule for future reviews — what should Claude remember]",
  "confidence": "[high | medium | low — set automatically per D-08]",
  "date": "[today's date as YYYY-MM-DD]"
}
```

**Confidence assignment (D-08):**
- `high`: user explicitly confirmed the correction, OR it is a clear structural fact (empty translation, wrong variable for language, broken HTML tag)
- `medium`: correction is valid but context-dependent, involves linguistic nuance, or applies to tone/formality
- `low`: flagged as speculative, needs native speaker verification, or inferred from ambiguous feedback

### 7c — Update config files if applicable

- If the rule touches variable usage -> also update `config/label_patterns.json` > `subject_variable_usage_rules`
- If the rule touches tone/formality -> also update `config/tone_guidelines.json`

### 7d — Rebuild rules_summary.json

Read ALL entries in `corrections/corrections_log.json` > `corrections`. Rebuild `corrections/rules_summary.json` from scratch (per D-10 — full rebuild, no append):

For each correction entry:
1. Check if a rule already exists in the rebuild with same `language` + same `issue_category` + semantically similar `rule_extracted` (per D-11)
2. If match: increment `occurrence_count`, update `last_seen` to the later date, update `confidence` to the highest level seen (high > medium > low)
3. If no match: create new rule entry:
```json
{
  "rule": "[the rule_extracted text]",
  "language": "[language code, or 'all' for universal patterns]",
  "issue_category": "[category]",
  "occurrence_count": 1,
  "confidence": "[confidence level]",
  "first_seen": "[date from correction]",
  "last_seen": "[date from correction]"
}
```

Write the full file:
```json
{
  "generated": "[today's date YYYY-MM-DD]",
  "total_rules": [N],
  "rules": [... all rule entries ...]
}
```

Announce: "Rules summary updated: [N] rules across [M] languages -> corrections/rules_summary.json" (per D-12)

### 7e — Confirm session

"Updated [N] rules. Config files updated: [list or 'none']."
```

IMPORTANT: Do NOT modify any other step in the skill file (Steps 0-6). Only replace the content after the numbered index listing in Step 7.

Also update the Step 4c line 100 reference from `corrections/corrections_log.json` to remove the mention — Step 4c currently says "Read ... the relevant rules from `corrections/corrections_log.json` once." This line at the top of Step 4c (around line 100) should be changed to read: "Read `config/review_rules_compact.md` once." (removing the corrections_log.json reference, since Step 3 now handles rule loading separately via rules_summary.json — this change prepares for Plan 03-02).
  </action>
  <verify>
    <automated>cd "/Users/juan/Desktop/Superprof Projects/Product quality review automation" && python3 -c "
content = open('.claude/commands/review-translations.md').read()
checks = [
    ('7a' in content, 'Step 7a sub-section exists'),
    ('7b' in content, 'Step 7b sub-section exists'),
    ('7c' in content, 'Step 7c sub-section exists'),
    ('7d' in content, 'Step 7d sub-section exists'),
    ('7e' in content, 'Step 7e sub-section exists'),
    ('Conflict detected before writing' in content, 'Conflict block template present'),
    ('issue_category' in content, 'New field name issue_category present'),
    ('notification_type' in content, 'New field name notification_type present'),
    ('rule_extracted' in content, 'New field name rule_extracted present'),
    ('confidence' in content, 'confidence field present'),
    ('high | medium | low' in content or 'high.*medium.*low' in content, 'confidence values documented'),
    ('one of: grammar | tone | label | cultural | emoji | encoding | format' in content, 'issue_category enum present'),
    ('D-06' in content or 'dismissal reason' in content, 'False-positive handling documented'),
    ('D-07' in content or 'ONE entry per market' in content, 'Per-market write documented'),
    ('D-08' in content or 'set automatically' in content, 'Auto-confidence documented'),
    ('rules_summary.json' in content, 'rules_summary.json referenced'),
    ('full rebuild' in content or 'from scratch' in content, 'Full rebuild documented'),
    ('Which takes precedence' in content, 'Conflict resolution options present'),
]
failed = [msg for (ok, msg) in checks if not ok]
if failed:
    print('FAIL:', failed)
    exit(1)
print(f'PASS: all {len(checks)} checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - review-translations.md contains sub-sections "7a", "7b", "7c", "7d", "7e" within Step 7
    - review-translations.md contains the string "Conflict detected before writing" (conflict block template)
    - review-translations.md Step 7 references field names: "issue_category", "notification_type", "rule_extracted", "confidence"
    - review-translations.md Step 7 contains "one of: grammar | tone | label | cultural | emoji | encoding | format"
    - review-translations.md Step 7 contains "ONE entry per market" or equivalent per-market write instruction (D-07)
    - review-translations.md Step 7 contains confidence auto-assignment rules with high/medium/low (D-08)
    - review-translations.md Step 7 contains "rules_summary.json" reference for the rebuild step (D-10)
    - review-translations.md Step 7 contains "Which takes precedence" with 3 numbered options (D-19)
    - review-translations.md Step 4c no longer references "corrections/corrections_log.json" for rule loading
    - Steps 0 through 6 are unchanged (diff should show no modifications outside Step 7 and the one Step 4c line)
  </acceptance_criteria>
  <done>Step 7 writes structured 8-field entries per market, performs pre-write conflict detection, rebuilds rules_summary.json, and Step 4c no longer loads corrections_log.json directly</done>
</task>

</tasks>

<verification>
1. `python3 -c "import json; d=json.load(open('corrections/corrections_log.json')); assert set(d['_schema']['correction'].keys()) == {'language','notification_type','issue_category','original','corrected','rule_extracted','confidence','date'}; assert 'rules_summary' not in d; print('Schema OK')"` passes
2. `grep -c 'Conflict detected before writing' .claude/commands/review-translations.md` returns 1
3. `grep -c 'issue_category' .claude/commands/review-translations.md` returns at least 3 (schema doc + Step 7 entry + rebuild)
4. `grep 'corrections/corrections_log.json' .claude/commands/review-translations.md` does NOT appear in Step 4c context (only in Step 7 write target)
</verification>

<success_criteria>
- corrections_log.json has exactly the 8-field schema with no rules_summary array
- Step 7 in review-translations.md writes structured per-market entries with all 8 fields
- Pre-write conflict detection is implemented with the 3-option conflict block
- Confidence is set automatically by Claude based on D-08 criteria
- rules_summary.json rebuild is triggered after every feedback session
</success_criteria>

<output>
After completion, create `.planning/phases/03-feedback-loop-strengthening/03-01-SUMMARY.md`
</output>
