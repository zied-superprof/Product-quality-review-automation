---
phase: 03-feedback-loop-strengthening
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .claude/commands/review-translations.md
autonomous: true
requirements:
  - FBK-03
  - FBK-04

must_haves:
  truths:
    - "After a Step 7 feedback session, corrections/rules_summary.json exists with generated date, total_rules count, and rules array"
    - "At the start of an AI review, Step 3 reads rules_summary.json (not corrections_log.json) and surfaces top-3 rules per language"
    - "Rules are scored by occurrence_count x recency_weight x confidence_score and only top-5 per language loaded into context"
    - "If a language has fewer than 3 specific rules, padding from 'all' language rules fills the remaining slots"
  artifacts:
    - path: ".claude/commands/review-translations.md"
      provides: "Rewritten Step 3 with relevance-scored rule retrieval from rules_summary.json"
      contains: "recency_weight"
  key_links:
    - from: ".claude/commands/review-translations.md"
      to: "corrections/rules_summary.json"
      via: "Step 3 reads rules_summary.json"
      pattern: "rules_summary.json"
    - from: ".claude/commands/review-translations.md"
      to: "corrections/rules_summary.json"
      via: "Step 7d rebuilds rules_summary.json"
      pattern: "rules_summary.json"
---

<objective>
Rewrite Step 3 of review-translations.md to load rules from rules_summary.json with relevance scoring and top-3 per-language surfacing.

Purpose: FBK-03 is structurally handled by Plan 03-01's Step 7d rebuild logic — this plan ensures the file is correctly consumed. FBK-04 replaces undifferentiated rule loading with scored, per-language top-3 retrieval so the AI review receives only the most relevant past corrections.
Output: Rewritten Step 3 in review-translations.md.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-feedback-loop-strengthening/03-CONTEXT.md
@.planning/phases/03-feedback-loop-strengthening/03-RESEARCH.md
@.planning/phases/03-feedback-loop-strengthening/03-01-SUMMARY.md

@.claude/commands/review-translations.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Step 3 for relevance-scored rule retrieval from rules_summary.json (FBK-03, FBK-04, D-09 through D-16)</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (current file after Plan 03-01 — find Step 3 section "## Step 3: Load corrections history")
    - .planning/phases/03-feedback-loop-strengthening/03-CONTEXT.md (D-09 through D-16)
    - .planning/phases/03-feedback-loop-strengthening/03-RESEARCH.md (Pattern 2: rules_summary.json Schema, Pattern 3: Relevance Scoring Algorithm, Step 3 Replacement Logic)
    - .planning/phases/03-feedback-loop-strengthening/03-01-SUMMARY.md (confirm Step 7 rebuild logic is in place)
  </read_first>
  <action>
Replace the entire Step 3 section in review-translations.md. The current Step 3 (after Plan 03-01) reads corrections_log.json and filters rules_summary — replace it with rules_summary.json loading and relevance scoring.

Find the section starting with "## Step 3: Load corrections history" and ending before "## Step 4: AI quality review". Replace it with:

```markdown
## Step 3: Load learned rules

Read `corrections/rules_summary.json`.

If the file does not exist or is empty, skip this step silently — no rules to apply.

If `total_rules` exceeds 150, log one line: "rules_summary.json has grown large ([N] rules) — consider pruning low-confidence entries." (per D-16)

**Relevance scoring** (per D-13):

For each rule in the `rules` array, compute:
- `confidence_score`: high = 1.0, medium = 0.75, low = 0.5
- `recency_weight`: calculate days between today and `last_seen`:
  - 0-30 days -> 1.0
  - 31-90 days -> 0.8
  - 91+ days -> 0.6
- `relevance_score` = `occurrence_count` x `recency_weight` x `confidence_score`

**Per-language rule selection** (per D-14, D-16):

For each language in the review batch:
1. Filter rules where `rule.language` == target language code. Score and sort descending by `relevance_score`. Take top-5 (per D-16 cap).
2. If fewer than 3 language-specific rules: pad with the highest-scoring rules where `rule.language` == `"all"` (scored with the same formula), until 3 total rules reached — or all available if fewer than 3 exist total.
3. The top-3 from this combined list become additional review criteria in Step 4c criterion 7 ("Past corrections").

**Output** (per D-15): Rules are loaded silently — no per-language announcement. After all languages processed, if any rules were loaded, output one line: "Applying [N] learned rules from previous reviews."
```

Also verify that Step 4c (around the line that was modified in Plan 03-01) references `config/review_rules_compact.md` only — NOT `corrections/corrections_log.json`. If the old reference persists, remove it. Step 4c criterion 7 ("Past corrections") should reference "rules loaded in Step 3" rather than loading its own copy.

Check Step 4c criterion 7 description (line ~118 in current file): "7. Past corrections — does this repeat a rule from corrections history?" — update to: "7. Past corrections — apply the top-3 rules loaded in Step 3 for this language. Flag if the translation repeats a known past error."
  </action>
  <verify>
    <automated>cd "/Users/juan/Desktop/Superprof Projects/Product quality review automation" && python3 -c "
content = open('.claude/commands/review-translations.md').read()
checks = [
    ('rules_summary.json' in content, 'rules_summary.json referenced'),
    ('recency_weight' in content, 'recency_weight formula present'),
    ('confidence_score' in content, 'confidence_score mapping present'),
    ('occurrence_count' in content, 'occurrence_count used in scoring'),
    ('relevance_score' in content or 'relevance scoring' in content.lower(), 'relevance scoring documented'),
    ('0.75' in content, 'medium confidence value 0.75 present'),
    ('0.5' in content, 'low confidence value 0.5 present'),
    ('0.8' in content, 'recency weight 0.8 for 31-90 days present'),
    ('0.6' in content, 'recency weight 0.6 for 91+ days present'),
    ('150' in content, 'Rule count warning threshold 150 present'),
    ('top-3' in content or 'top 3' in content.lower(), 'top-3 rule surfacing documented'),
    ('top-5' in content or 'top 5' in content.lower(), 'top-5 per-language cap documented'),
    ('\"all\"' in content or \"'all'\" in content, 'all-language padding documented'),
    ('Applying' in content and 'learned rules' in content, 'Silent output with count present'),
    ('Load learned rules' in content or 'Load corrections' in content, 'Step 3 header present'),
]

# Verify Step 3 does NOT reference corrections_log.json for reading
step3_start = content.find('## Step 3')
step4_start = content.find('## Step 4')
if step3_start >= 0 and step4_start >= 0:
    step3_content = content[step3_start:step4_start]
    checks.append(('corrections_log.json' not in step3_content, 'Step 3 does NOT read corrections_log.json'))
else:
    checks.append((False, 'Could not isolate Step 3 section'))

failed = [msg for (ok, msg) in checks if not ok]
if failed:
    print('FAIL:', failed)
    exit(1)
print(f'PASS: all {len(checks)} checks passed')
"</automated>
  </verify>
  <acceptance_criteria>
    - review-translations.md Step 3 header reads "Load learned rules" (not "Load corrections history")
    - review-translations.md Step 3 contains "rules_summary.json" (the file it reads)
    - review-translations.md Step 3 does NOT contain "corrections_log.json" (old read target removed)
    - review-translations.md Step 3 contains the string "recency_weight" with values 1.0, 0.8, 0.6
    - review-translations.md Step 3 contains the string "confidence_score" with values 1.0, 0.75, 0.5
    - review-translations.md Step 3 contains "occurrence_count" in the scoring formula
    - review-translations.md Step 3 contains "150" as the warning threshold
    - review-translations.md Step 3 documents top-3 surfacing and top-5 cap
    - review-translations.md Step 3 documents padding from "all" language rules
    - review-translations.md Step 3 ends with one-line output pattern "Applying [N] learned rules from previous reviews."
    - review-translations.md Step 4c criterion 7 references "rules loaded in Step 3" (not direct file loading)
  </acceptance_criteria>
  <done>Step 3 reads rules_summary.json, scores rules by occurrence x recency x confidence, surfaces top-3 per language with all-language padding, and Step 4c criterion 7 references Step 3 output</done>
</task>

</tasks>

<verification>
1. `grep -c 'rules_summary.json' .claude/commands/review-translations.md` returns at least 3 (Step 3 read + Step 7d write + at least one more reference)
2. `grep 'recency_weight' .claude/commands/review-translations.md` returns at least 1 match
3. `grep 'confidence_score' .claude/commands/review-translations.md` returns at least 1 match
4. Step 3 section (between "## Step 3" and "## Step 4") does NOT contain "corrections_log.json"
5. Step 4c does NOT contain "corrections/corrections_log.json" for rule reading
</verification>

<success_criteria>
- Step 3 reads only rules_summary.json (not corrections_log.json)
- Rules are scored using the D-13 formula: occurrence_count x recency_weight x confidence_score
- Top-3 rules per language are surfaced, with all-language padding for languages with fewer than 3 specific rules
- Top-5 per-language cap is enforced (D-16)
- 150-rule warning threshold is implemented (D-16)
- Rules are loaded silently with a single count announcement (D-15)
</success_criteria>

<output>
After completion, create `.planning/phases/03-feedback-loop-strengthening/03-02-SUMMARY.md`
</output>
