---
phase: 01-token-optimization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/review-translations.md
  - reports/token-baseline.md
autonomous: true
requirements:
  - TOK-01
  - TOK-03

must_haves:
  truths:
    - "Step 4c no longer outputs JSON arrays to the conversation — only one progress line per market"
    - "A baseline token metric artifact exists documenting pre-optimization token cost"
    - "Step 5 merge still receives the full accumulated findings list"
  artifacts:
    - path: "reports/token-baseline.md"
      provides: "Before/after token usage record"
      contains: "## Baseline"
    - path: ".claude/commands/review-translations.md"
      provides: "Silent accumulation instruction in Step 4c"
      contains: "Do NOT output JSON to the conversation"
  key_links:
    - from: ".claude/commands/review-translations.md (Step 4c)"
      to: ".claude/commands/review-translations.md (Step 5)"
      via: "ai_findings accumulated list"
      pattern: "ai_findings"
---

<objective>
Eliminate context-window waste in Step 4c of the review-translations skill and establish a baseline token metric.

Purpose: Step 4c currently outputs a full JSON array per market to the conversation. On a 39-language batch this floods 5,000-30,000 tokens of raw JSON that must be re-read in Step 5. Silent accumulation eliminates this waste. The baseline metric (TOK-03) must be recorded BEFORE changes so the improvement is measurable.
Output: Modified review-translations.md with silent accumulation, reports/token-baseline.md with pre/post metric.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-token-optimization/01-RESEARCH.md
@.claude/commands/review-translations.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish baseline token metric (TOK-03)</name>
  <files>reports/token-baseline.md</files>
  <read_first>
    - .claude/commands/review-translations.md (current Step 4c instruction — lines 75-95 — to document what produces token waste)
  </read_first>
  <action>
Create `reports/token-baseline.md` documenting the pre-optimization token cost. The file must contain:

1. A `## Baseline` section with:
   - Date: 2026-04-08
   - Description of the token waste source: "Step 4c outputs one JSON array per market to the conversation context. Each array contains issue objects with fields: market, lang, severity, category, issue, original_fr, current_translation, suggested_fix. On a 39-market batch, this produces 5,000-30,000 tokens of inline JSON that the model re-reads as input context in Step 5."
   - Estimated token cost formula: "Per market: ~50-500 tokens (empty array = ~10 tokens, market with 5 issues = ~500 tokens). For 39 markets: ~2,000-20,000 output tokens echoed, then re-consumed as ~2,000-20,000 input tokens in Step 5."
   - Current Step 4c instruction (quote lines 79 and 95 verbatim from review-translations.md)

2. A `## Post-Optimization` section with placeholder: "To be filled after TOK-01 changes are applied."

3. A `## Measurement Method` section: "Compare conversation length (in tokens displayed by Claude Code) for the same CSV file before and after. The JSON arrays in Step 4c are replaced by one-line progress indicators (~15 tokens per market), saving an estimated 80-95% of Step 4c output tokens."

This task MUST be completed and its output committed before Task 2 modifies review-translations.md, to ensure the baseline is recorded against the unmodified skill.
  </action>
  <verify>
    <automated>grep -c "## Baseline" reports/token-baseline.md && grep -c "## Post-Optimization" reports/token-baseline.md && grep -c "## Measurement Method" reports/token-baseline.md</automated>
  </verify>
  <acceptance_criteria>
    - reports/token-baseline.md exists
    - File contains "## Baseline" heading
    - File contains "## Post-Optimization" heading
    - File contains "## Measurement Method" heading
    - File contains the verbatim current instruction: "output a JSON array of issues before moving to the next"
    - File contains estimated token range: "5,000-30,000"
  </acceptance_criteria>
  <done>Baseline token metric artifact exists with pre-optimization cost documented, post-optimization placeholder ready</done>
</task>

<task type="auto">
  <name>Task 2: Implement silent accumulation in Step 4c (TOK-01)</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (full file — understand the complete Step 4c and Step 5 flow before editing)
    - .planning/phases/01-token-optimization/01-RESEARCH.md (Pattern 1: Silent Accumulation — lines 72-99 — for the exact replacement text and anti-patterns)
  </read_first>
  <action>
Edit `.claude/commands/review-translations.md` Step 4c (lines 75-95). Make these specific changes:

**Replace line 79** (currently: "Work through each flagged market **inline in this conversation** — no subagents, no batching. For each market, evaluate the 7 criteria below and output a JSON array of issues before moving to the next.")

With: "Work through each flagged market **inline in this conversation** — no subagents, no batching. For each market, evaluate the 7 criteria below and **append all issues to `ai_findings`** (a running flat list held in memory). Do NOT output JSON arrays to the conversation. Instead, output one progress line per market:"

**Add after the replacement line:**
```
"Reviewed [Country] ([lang]) — [N] issues found."
```

**Replace line 95** (currently: "After all markets are reviewed, merge all JSON arrays into a single flat list. This is the AI findings set for Step 5.")

With: "After all markets are reviewed, output a summary line: 'AI review complete: [N] markets reviewed, [M] total issues found.' The `ai_findings` list is the AI findings set for Step 5."

**Keep unchanged:**
- The 7 criteria list (lines 83-89)
- The JSON schema for individual issues (lines 91-93) — this remains the shape of each object in `ai_findings`, it is just not echoed to the conversation
- The `**Output per market**` label on line 90 — change it to `**Issue schema** (each object appended to `ai_findings`):`

**Do NOT:**
- Remove the JSON issue schema — it defines the shape of each finding object
- Change Step 5 merge logic — it already expects "a single flat list"
- Add any new steps or sub-steps
  </action>
  <verify>
    <automated>grep -c "Do NOT output JSON" .claude/commands/review-translations.md && grep -c "ai_findings" .claude/commands/review-translations.md && grep -c "Reviewed \[Country\]" .claude/commands/review-translations.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/review-translations.md contains "Do NOT output JSON arrays to the conversation"
    - .claude/commands/review-translations.md contains "append all issues to `ai_findings`"
    - .claude/commands/review-translations.md contains "Reviewed [Country] ([lang])"
    - .claude/commands/review-translations.md contains "AI review complete:"
    - .claude/commands/review-translations.md does NOT contain "output a JSON array of issues before moving to the next" (the old instruction)
    - .claude/commands/review-translations.md still contains the 7 criteria (grep for "Grammar" and "Label correctness")
    - .claude/commands/review-translations.md still contains the JSON issue schema fields: "market", "severity", "category", "suggested_fix"
    - Step 5 section still references "AI findings" or "ai_findings"
  </acceptance_criteria>
  <done>Step 4c accumulates findings silently into ai_findings list with one progress line per market; Step 5 merge receives the same flat list it always did; no JSON arrays appear in the conversation during review</done>
</task>

</tasks>

<verification>
1. `grep "Do NOT output JSON" .claude/commands/review-translations.md` returns a match
2. `grep "ai_findings" .claude/commands/review-translations.md` returns at least 3 matches (instruction, schema label, Step 5 handoff)
3. `grep "output a JSON array of issues before moving to the next" .claude/commands/review-translations.md` returns NO match (old instruction removed)
4. `reports/token-baseline.md` exists with all three sections
</verification>

<success_criteria>
- The review-translations.md skill instructs Claude to accumulate findings silently — no JSON arrays echo to the conversation during Step 4c
- A baseline token metric file exists documenting the pre-optimization state
- Step 5 merge continues to work against the same flat list of findings
</success_criteria>

<output>
After completion, create `.planning/phases/01-token-optimization/01-01-SUMMARY.md`
</output>
