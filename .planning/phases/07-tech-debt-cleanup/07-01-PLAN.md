---
phase: 07-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/review-translations.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [TOK-02, RPT-03]
gap_closure: true

must_haves:
  truths:
    - "Step 2 structural validator call includes --summary flag so only market names and issue counts print to conversation"
    - "RPT-03 requirement prose accurately describes .md as the default output format"
    - "No stale plan-06-02 comment exists in review-translations.md"
  artifacts:
    - path: ".claude/commands/review-translations.md"
      provides: "Step 2 call with --summary flag, clean Step 7b-batch prose"
      contains: "--summary"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate RPT-03 description"
      contains: ".md output is generated by default"
  key_links:
    - from: ".claude/commands/review-translations.md (Step 2)"
      to: "scripts/structural_validator.py --summary"
      via: "bash command invocation"
      pattern: "--output reports/structural_results.json --summary"
---

<objective>
Close three tech debt items flagged by the v1.1 milestone audit: wire the --summary flag into the Step 2 call site (realizing the token savings TOK-02 was designed to deliver), update the stale RPT-03 requirement prose, and remove a stale internal comment.

Purpose: The --summary flag was built in Phase 1 but never wired into the skill's Step 2 call. Full JSON arrays still flow into conversation context during structural validation, negating the 80-90% token reduction the flag was designed to deliver. The other two items are documentation accuracy fixes.
Output: Two files edited with three 1-line changes total.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/07-tech-debt-cleanup/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire --summary flag and remove stale comment in review-translations.md</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (full file context; key lines: 63 for Step 2 command, 474 for stale comment)
    - .planning/v1.1-MILESTONE-AUDIT.md (source of truth for both changes)
  </read_first>
  <action>
Two edits in `.claude/commands/review-translations.md`:

**Edit 1 — Line 63: Append --summary to structural_validator.py call (per D-01, D-02)**

BEFORE:
```
python3 scripts/structural_validator.py --input [CSV_PATH] --type [per-notification|full-database] --config-dir config/ --output reports/structural_results.json
```

AFTER:
```
python3 scripts/structural_validator.py --input [CSV_PATH] --type [per-notification|full-database] --config-dir config/ --output reports/structural_results.json --summary
```

Only `--summary` is appended at the end. No other changes to the line.

**Edit 2 — Line 474: Remove stale parenthetical comment (per D-06, D-07)**

BEFORE:
```
After the block list, show the confirmation prompt (the apply logic is implemented in plan 06-02):
```

AFTER:
```
After the block list, show the confirmation prompt:
```

Only the parenthetical is removed. The surrounding prose stays intact.
  </action>
  <verify>
    <automated>grep -n "\-\-summary" ".claude/commands/review-translations.md" && ! grep -q "plan 06-02" ".claude/commands/review-translations.md" && echo "PASS: --summary present, stale comment removed"</automated>
  </verify>
  <acceptance_criteria>
    - `grep --summary .claude/commands/review-translations.md` returns the Step 2 command line containing `--output reports/structural_results.json --summary`
    - `grep "plan 06-02" .claude/commands/review-translations.md` returns no matches (exit code 1)
    - The line formerly at 474 now reads exactly: `After the block list, show the confirmation prompt:`
  </acceptance_criteria>
  <done>Step 2 structural validator call includes --summary flag. Stale plan-06-02 comment is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Update RPT-03 requirement prose in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (key line: 24 for RPT-03)
    - .planning/v1.1-MILESTONE-AUDIT.md (confirms RPT-03 prose is stale and needs updating)
  </read_first>
  <action>
One edit in `.planning/REQUIREMENTS.md`:

**Line 24: Update RPT-03 prose to reflect .md default (per D-04, D-05)**

BEFORE:
```
- [x] **RPT-03**: HTML output is generated by default (human-readable in any browser, no special software, no markdown knowledge required)
```

AFTER:
```
- [x] **RPT-03**: Markdown output is generated by default (lightweight, version-controllable, and convertible to Notion or PDF as needed). Superseded by NTIO-04: HTML removed in Phase 5; .md is now the sole default format.
```

The `[x]` marker stays because the requirement is satisfied (intentionally superseded, not failed). The new prose accurately describes the current .md-default behavior and cross-references the superseding requirement.
  </action>
  <verify>
    <automated>grep "RPT-03" ".planning/REQUIREMENTS.md" | grep -q "Markdown output is generated by default" && echo "PASS: RPT-03 prose updated"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "RPT-03" .planning/REQUIREMENTS.md` returns a line containing "Markdown output is generated by default"
    - The line still begins with `- [x]` (completion marker preserved)
    - The line contains "Superseded by NTIO-04" (cross-reference to the change that made it stale)
    - No occurrence of "HTML output is generated by default" remains in the file
  </acceptance_criteria>
  <done>RPT-03 requirement prose accurately reflects .md as the default output format and references the superseding NTIO-04 change.</done>
</task>

</tasks>

<verification>
All three audit gaps closed:
1. `grep "\-\-summary" .claude/commands/review-translations.md` — confirms --summary is in Step 2 call
2. `grep "plan 06-02" .claude/commands/review-translations.md` — confirms stale comment removed (no matches)
3. `grep "RPT-03" .planning/REQUIREMENTS.md` — confirms prose says "Markdown output" not "HTML output"
</verification>

<success_criteria>
- The Step 2 structural_validator.py command includes `--summary`, so future review runs print only market names and issue counts (not full JSON arrays) to the conversation
- REQUIREMENTS.md RPT-03 prose says "Markdown output is generated by default" with a cross-reference to NTIO-04
- No occurrence of "the apply logic is implemented in plan 06-02" exists in review-translations.md
- No other lines in either file are changed
</success_criteria>

<output>
After completion, create `.planning/phases/07-tech-debt-cleanup/07-01-SUMMARY.md`
</output>
