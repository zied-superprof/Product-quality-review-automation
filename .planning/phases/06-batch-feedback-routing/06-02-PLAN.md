---
phase: 06-batch-feedback-routing
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .claude/commands/review-translations.md
autonomous: true
requirements:
  - FBK-07

must_haves:
  truths:
    - "User types item numbers (e.g. 1, 3, 4) and only those items are written"
    - "Confirmed items targeting corrections_log.json are written as 8-field entries via 7b"
    - "Confirmed items targeting label_patterns.json update subject_variable_usage_rules via 7c"
    - "Confirmed items targeting tone_guidelines.json update formality_rules via 7c"
    - "rules_summary.json is rebuilt exactly once after all writes complete (7d)"
    - "A change summary lists every file modified and what changed"
    - "Items with conflicts are excluded from confirmation until resolved via discussion"
    - "Items not listed in the confirmation are silently discarded"
  artifacts:
    - path: ".claude/commands/review-translations.md"
      provides: "Step 7b-batch confirmation, write, conflict resolution, and summary"
      contains: "7b-batch"
  key_links:
    - from: ".claude/commands/review-translations.md Step 7b-batch"
      to: "corrections/corrections_log.json"
      via: "7b write per confirmed item"
      pattern: "corrections_log.json"
    - from: ".claude/commands/review-translations.md Step 7b-batch"
      to: "config/label_patterns.json"
      via: "7c config update per confirmed item"
      pattern: "label_patterns.json"
    - from: ".claude/commands/review-translations.md Step 7b-batch"
      to: "config/tone_guidelines.json"
      via: "7c config update per confirmed item"
      pattern: "tone_guidelines.json"
    - from: ".claude/commands/review-translations.md Step 7b-batch"
      to: "corrections/rules_summary.json"
      via: "7d rebuild once after all writes"
      pattern: "rules_summary.json"
---

<objective>
Add confirmation parsing, one-pass write execution, conflict resolution, and change summary to the batch feedback flow in Step 7.

Purpose: Completes FBK-07 (user confirms/rejects per item, confirmed actions applied immediately) by wiring the batch routing analysis from plan 01 into the existing 7b/7c/7d write infrastructure.

Output: Modified `.claude/commands/review-translations.md` with the full batch apply flow: number-based confirmation, sequential writes reusing 7b/7c, single 7d rebuild, conflict resolution discussion, and change summary.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-batch-feedback-routing/06-CONTEXT.md
@.planning/phases/06-batch-feedback-routing/06-RESEARCH.md
@.planning/phases/06-batch-feedback-routing/06-01-SUMMARY.md
@.claude/commands/review-translations.md
@corrections/corrections_log.json
@config/label_patterns.json
@config/tone_guidelines.json

<interfaces>
<!-- After plan 01, the skill contains:
  - "### 7a-batch — Batch routing analysis" section with parsing, routing, conflict check, and block list display
  - The block list ends with a confirmation prompt: "Enter the item numbers you want to apply..."
  - "### 7a-single" (renamed from 7a) with the original single-item conflict check
  - Existing 7b, 7c, 7d, 7e sections unchanged

Plan 02 adds a new "### 7b-batch" section after 7a-batch that handles:
  - Parsing the user's number selection
  - Running 7b write for each confirmed item
  - Running 7c config update for each confirmed item that targets label_patterns or tone_guidelines
  - Running 7d rebuild once
  - Conflict resolution discussion for blocked items
  - Change summary output
-->

Existing write infrastructure (reused, not rewritten):
- 7b: Write ONE entry per market to corrections/corrections_log.json > corrections array. 8 fields: language, notification_type, issue_category, original, corrected, rule_extracted, confidence, date.
- 7c: If rule touches variable usage -> update label_patterns.json > subject_variable_usage_rules. If rule touches tone/formality -> update tone_guidelines.json.
- 7d: Full rebuild of corrections/rules_summary.json from all entries in corrections_log.json.
- 7e: Confirm session output: "Updated [N] rules. Config files updated: [list or 'none']."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add confirmation parsing, one-pass write, and change summary (7b-batch)</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (re-read full file — especially the new 7a-batch section from plan 01, and existing 7b/7c/7d/7e sections)
    - .planning/phases/06-batch-feedback-routing/06-CONTEXT.md (decisions D-07 through D-10)
    - .planning/phases/06-batch-feedback-routing/06-RESEARCH.md (Pattern 5: One-Pass Write, Pitfall 2: Running 7d Per Item)
    - corrections/corrections_log.json (current schema and _schema object)
  </read_first>
  <action>
Add a new `### 7b-batch — Confirm and apply` section to `.claude/commands/review-translations.md`. Place it AFTER the 7a-batch section (which ends with the confirmation prompt) and BEFORE the existing 7a-single section.

**Section content:**

```
### 7b-batch — Confirm and apply

When the user responds to the batch confirmation prompt with item numbers:

#### Parse confirmation

Extract the item numbers from the user's reply. Accept formats: `1, 3, 4` or `1 3 4` or `1,3,4`. Ignore any non-numeric text.

**Validation:**
- If a number refers to an item marked with ⚠️ conflict, reject it: "Item #[N] has an unresolved conflict. Resolve it first (see below) or omit it."
- If a number refers to a Variables.csv flag-only item, reject it: "Item #[N] is a Variables.csv flag — it cannot be applied automatically."
- If a number does not correspond to any item in the block list, ignore it silently.
- Items not listed in the confirmation are silently discarded — no pending queue (per D-09).

#### Write confirmed items (one pass, per D-10)

Process each confirmed item in order:

For each confirmed item:
1. **If destination is `corrections_log.json`**: Write a structured entry using the same 7b logic (8-field schema). Use these field values:
   - `language`: the parsed language code from the batch item
   - `notification_type`: `"batch-feedback"`
   - `issue_category`: infer from the issue text (one of: grammar, tone, label, cultural, emoji, encoding, format)
   - `original`: the issue text as submitted by the user
   - `corrected`: the resolution or rule derived from the issue (e.g., "FALSE POSITIVE — vos is Rioplatense brand standard for es_AR")
   - `rule_extracted`: a generalized rule for future reviews
   - `confidence`: assign per 7b rules (high for explicit confirmations and structural facts, medium for linguistic nuance, low for speculative)
   - `date`: today's date as YYYY-MM-DD

2. **If destination is `label_patterns.json`**: Update `subject_variable_usage_rules` using the same 7c logic. Depending on the issue:
   - Add a language code to `use_for` or `do_not_use_for` for the relevant variable
   - Add a `language_notes` entry for the language
   Also write a corrections_log.json entry (step 1 above) to record the rule.

3. **If destination is `tone_guidelines.json`**: Update `formality_rules` using the same 7c logic. Depending on the issue:
   - Add or move a language code between `informal_standard_languages.languages` and `formal_vous_languages.languages`
   - Add a `market_notes` entry if the issue is market-specific (e.g., es_AR vos pattern)
   Also write a corrections_log.json entry (step 1 above) to record the rule.

**Multi-market split**: If a batch item was split into multiple markets during parsing (7a-batch), each market gets its own corrections_log.json entry — one entry per market per item, per the established D-07 rule (language is always a single string).

#### Rebuild rules_summary.json (once)

After ALL confirmed items have been written, run the 7d rebuild logic exactly once:
- Read ALL entries in `corrections/corrections_log.json` > `corrections`
- Rebuild `corrections/rules_summary.json` from scratch (full rebuild, not append)
- Follow the same deduplication and scoring logic as the existing 7d section

Do NOT run 7d after each item — run it once at the end (avoiding Pitfall 2 from RESEARCH.md).

#### Output change summary

After all writes and the rebuild, output a summary:

```
Batch feedback applied:
- [N] corrections written to corrections_log.json
- [M] config updates: [list of files updated, e.g. "label_patterns.json (2 rules), tone_guidelines.json (1 rule)"]
- rules_summary.json rebuilt: [total_rules] rules across [languages] languages
- [K] items discarded (not confirmed)
- [J] items flagged as Variables.csv warnings (informational only)
```

If no items were confirmed (user typed nothing or only invalid numbers), output: "No items confirmed. Batch feedback session ended."
```
  </action>
  <verify>
    <automated>grep -c "7b-batch" ".claude/commands/review-translations.md" && grep -c "Confirm and apply" ".claude/commands/review-translations.md" && grep -c "batch-feedback" ".claude/commands/review-translations.md" && grep -c "Batch feedback applied" ".claude/commands/review-translations.md" && grep -c "rules_summary.json rebuilt" ".claude/commands/review-translations.md"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/review-translations.md contains "### 7b-batch — Confirm and apply" section header
    - .claude/commands/review-translations.md contains "notification_type.*batch-feedback" pattern for batch-sourced entries
    - .claude/commands/review-translations.md contains "Batch feedback applied:" summary output format
    - .claude/commands/review-translations.md contains "rules_summary.json rebuilt:" in the summary
    - .claude/commands/review-translations.md contains "Do NOT run 7d after each item" or equivalent instruction about single rebuild
    - .claude/commands/review-translations.md contains the number parsing instructions ("1, 3, 4" or "1 3 4" or "1,3,4")
    - .claude/commands/review-translations.md contains validation for conflict items ("unresolved conflict")
    - .claude/commands/review-translations.md contains validation for Variables.csv flag items ("cannot be applied automatically")
    - The existing 7b, 7c, 7d, 7e sections are still present and unmodified (they are reused by reference, not replaced)
  </acceptance_criteria>
  <done>The 7b-batch section parses user-confirmed item numbers, writes each confirmed item using existing 7b/7c logic, rebuilds rules_summary once via 7d, and outputs a change summary listing all modifications.</done>
</task>

<task type="auto">
  <name>Task 2: Add conflict resolution discussion flow for blocked items</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (re-read after Task 1 — especially 7a-batch conflict display and 7b-batch confirmation parsing)
    - .planning/phases/06-batch-feedback-routing/06-CONTEXT.md (decisions D-05, D-06, D-07)
    - .planning/phases/06-batch-feedback-routing/06-RESEARCH.md (Pitfall 6: Discarding Conflict Context)
  </read_first>
  <action>
Add a `### 7c-batch — Conflict resolution` section to `.claude/commands/review-translations.md`. Place it AFTER the 7b-batch section and BEFORE the existing 7a-single section.

**Section content:**

```
### 7c-batch — Conflict resolution

After the batch apply (7b-batch) completes, if any items were marked with ⚠️ conflicts in the block list:

#### Present conflict details

For each conflicted item, show the full conflict context:

```
Conflict — Item #[N] ([language]: [brief issue]):

New rule:       "[the rule that would be written]"
Conflicts with: [filename] [section] — "[existing rule or config value]"

Let's discuss — what should we do with this conflict?
```

#### Collaborative resolution (per D-06)

Discuss the conflict with the user. There is no fixed menu — the resolution emerges from discussion. Common outcomes include:
- **Write the new rule anyway** (override the existing config): proceed with 7b/7c write for this item, then rebuild 7d.
- **Discard this item**: skip it entirely. Acknowledge: "Item #[N] discarded."
- **Update the existing config instead**: the user may ask to modify the existing rule/config entry rather than add a new one. Make the requested change to the relevant file, then rebuild 7d.

#### After resolution

After each conflict is resolved (or discarded):
- If a write was made, run 7d rebuild once more (single rebuild for all resolved items, not per item).
- Update the change summary with the resolution: "Conflict #[N] resolved: [action taken]."
- If more conflicts remain, present the next one.

When all conflicts are resolved or discarded, output:
"All conflicts addressed. Batch feedback session complete."

If there were no conflicts at all, skip this section entirely — go straight from 7b-batch summary to session end.
```
  </action>
  <verify>
    <automated>grep -c "7c-batch" ".claude/commands/review-translations.md" && grep -c "Conflict resolution" ".claude/commands/review-translations.md" && grep -c "Collaborative resolution" ".claude/commands/review-translations.md" && grep -c "All conflicts addressed" ".claude/commands/review-translations.md"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/review-translations.md contains "### 7c-batch — Conflict resolution" section header
    - .claude/commands/review-translations.md contains "Collaborative resolution" sub-heading
    - .claude/commands/review-translations.md contains the conflict display format with "New rule:" and "Conflicts with:" pattern
    - .claude/commands/review-translations.md contains "All conflicts addressed. Batch feedback session complete."
    - .claude/commands/review-translations.md contains reference to running 7d rebuild after conflict resolution
    - .claude/commands/review-translations.md contains all three resolution outcomes: write anyway, discard, update existing
    - The existing 7a-single, 7b, 7c, 7d, 7e sections are still present and unmodified
  </acceptance_criteria>
  <done>The full batch feedback flow is complete: 7a-batch (parse, route, display), 7b-batch (confirm, write, summary), 7c-batch (conflict resolution). The existing single-item flow (7a-single through 7e) is preserved unchanged. All three requirements (FBK-05, FBK-06, FBK-07) are implemented.</done>
</task>

</tasks>

<verification>
After both tasks:
1. The complete batch flow is: 7a-batch (detect + parse + route + display) -> 7b-batch (confirm + write + summary) -> 7c-batch (resolve conflicts if any)
2. The existing single-item flow is: 7a-single -> 7b -> 7c -> 7d -> 7e (completely unchanged)
3. Confirmed items are written using the exact same 7b schema and 7c config update logic as the single-item flow
4. rules_summary.json is rebuilt exactly once after all writes (not per item)
5. Variables.csv is never written to — only flagged (D-10 through D-13)
6. Multi-market items are split into one entry per market (D-07 from Phase 3)
7. Conflict items are excluded from confirmation until resolved via discussion (D-05, D-06)
8. The notification_type for batch entries is "batch-feedback"
</verification>

<success_criteria>
- User types item numbers after seeing the block list and only those items are applied
- Each confirmed item produces the correct write: corrections_log entry, and optionally label_patterns or tone_guidelines update
- rules_summary.json is rebuilt once at the end, not per item
- A clear change summary shows what was written and where
- Conflict items get a full-context discussion flow with flexible resolution options
- Items not confirmed are silently discarded
- The entire existing single-item feedback flow works exactly as before
</success_criteria>

<output>
After completion, create `.planning/phases/06-batch-feedback-routing/06-02-SUMMARY.md`
</output>
