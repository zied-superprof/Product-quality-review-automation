---
phase: 06-batch-feedback-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/commands/review-translations.md
autonomous: true
requirements:
  - FBK-05
  - FBK-06

must_haves:
  truths:
    - "User can paste multiple Language+Issue blocks into Step 7 and the system detects batch mode automatically"
    - "Each batch item gets a routing suggestion with destination file, rationale, and conflict status"
    - "Items mentioning @TPL_*@ variables route to label_patterns.json or flag-only for Variables.csv"
    - "Items mentioning formality/tone/register route to tone_guidelines.json"
    - "Items with no variable or tone signal route to corrections_log.json"
    - "Conflict detection runs per item before the block list is displayed"
    - "The existing single-item feedback flow (#N report numbers) still works unchanged"
  artifacts:
    - path: ".claude/commands/review-translations.md"
      provides: "Step 7 batch mode branch with routing and display"
      contains: "Step 7a-batch"
  key_links:
    - from: ".claude/commands/review-translations.md Step 7"
      to: "config/label_patterns.json"
      via: "conflict check reads subject_variable_usage_rules"
      pattern: "subject_variable_usage_rules"
    - from: ".claude/commands/review-translations.md Step 7"
      to: "config/tone_guidelines.json"
      via: "conflict check reads formality_rules"
      pattern: "formality_rules"
    - from: ".claude/commands/review-translations.md Step 7"
      to: "corrections/corrections_log.json"
      via: "default routing destination"
      pattern: "corrections_log.json"
---

<objective>
Add batch feedback detection and routing analysis to Step 7 of the review-translations skill. After this plan, a user can paste multiple Language+Issue blocks and see a block list with routing suggestions, conflict flags, and an employee collection template.

Purpose: Enables FBK-05 (batch submission) and FBK-06 (routing analysis with rationale and conflict detection) by extending the Step 7 entry point with format detection, a 4-bucket routing classifier, and conflict-aware block list display.

Output: Modified `.claude/commands/review-translations.md` with batch mode branch in Step 7 (detection + analysis + display). The confirmation/apply logic comes in plan 02.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-batch-feedback-routing/06-CONTEXT.md
@.planning/phases/06-batch-feedback-routing/06-RESEARCH.md
@.claude/commands/review-translations.md
@config/label_patterns.json
@config/tone_guidelines.json
@corrections/corrections_log.json

<interfaces>
<!-- The executor MUST read the full Step 7 section (lines 388-509) of review-translations.md -->
<!-- to understand the existing flow before inserting the batch branch. -->

Existing Step 7 structure:
- Line 388: "## Step 7: Feedback loop"
- Lines 389-411: Prompt, numbered index display, user reply handling
- Lines 413-445: 7a — Pre-write conflict check
- Lines 447-468: 7b — Write structured correction entry
- Lines 470-474: 7c — Update config files
- Lines 476-505: 7d — Rebuild rules_summary.json
- Lines 507-509: 7e — Confirm session

Routing destination files:
- corrections_log.json: 8-field schema (language, notification_type, issue_category, original, corrected, rule_extracted, confidence, date)
- label_patterns.json: subject_variable_usage_rules.[VARIABLE_NAME].{use_for, do_not_use_for, language_notes}
- tone_guidelines.json: formality_rules.{informal_standard_languages, formal_vous_languages}.{languages, market_notes}
- Variables.csv: read-only, flag-only destination (D-10 through D-13)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add batch mode detection and session-independence note to Step 7 entry point</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (full file — current Step 7 starts at line 388)
    - .planning/phases/06-batch-feedback-routing/06-CONTEXT.md (decisions D-01 through D-03)
    - .planning/phases/06-batch-feedback-routing/06-RESEARCH.md (Pattern 1: Session-Independent Entry, Step 7 Entry Point Detection)
  </read_first>
  <action>
Modify Step 7 in `.claude/commands/review-translations.md`. The changes go BETWEEN the existing Step 7 header and the existing "Would you like to give feedback?" prompt. The existing single-item flow remains intact — batch mode is an additional branch.

**1. Add session-independence note** right after the `## Step 7: Feedback loop` header (per D-03):

```
> **Batch mode available**: This step also works in a fresh session without an active report. Paste feedback items in the Language+Issue format below and the system will route them to the correct config files.
```

**2. Add employee collection template** (per D-02) right after the session-independence note:

```
### Collection template

Share this template with the employee gathering native speaker feedback:

---
TRANSLATION FEEDBACK TEMPLATE
Fill in one block per issue. Copy-paste as many blocks as needed.

Language: [ISO code — e.g. es_AR, ar, de, fr]
Issue: [Describe what the AI reviewer got wrong. Was it a false positive? Wrong variable? Wrong tone?]

Language:
Issue:
---
```

**3. Add batch detection logic** after the collection template, BEFORE the existing "Would you like to give feedback?" prompt. Insert a new sub-section:

```
### Step 7 — Batch mode detection

When the user provides input at Step 7, detect the format:

**Batch mode trigger**: User input contains one or more blocks matching the pattern `Language: [code]` followed by `Issue: [text]` (with optional blank lines between blocks). Even a single Language+Issue block triggers batch mode for consistency.

**Single-item mode trigger**: User input contains `#N` patterns (report item numbers like `#3 this variable is valid`). This is the existing flow — proceed to the numbered index display below.

**Ambiguous input**: If the input matches neither format clearly, ask: "Are you giving feedback on specific report items (#1, #2...) or submitting new feedback in Language+Issue format?"

If batch mode is detected, proceed to **Step 7a-batch** (below). If single-item mode, proceed to the existing flow (numbered index display and per-item processing).
```

**4. Preserve the existing flow**. The existing prompt ("Would you like to give feedback on this review?...") and the existing 7a through 7e sections stay exactly where they are. The batch detection section is inserted ABOVE the existing prompt as an additional pathway. Rename the existing 7a section header from `### 7a` to `### 7a-single` to distinguish it from the new `### 7a-batch` section (added in Task 2).

Do NOT modify any content inside the existing 7a-single through 7e sections — only rename 7a's header.
  </action>
  <verify>
    <automated>grep -c "Batch mode available" ".claude/commands/review-translations.md" && grep -c "Collection template" ".claude/commands/review-translations.md" && grep -c "Batch mode detection" ".claude/commands/review-translations.md" && grep -c "7a-single" ".claude/commands/review-translations.md" && grep -c "7a-batch" ".claude/commands/review-translations.md"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/review-translations.md contains the string "Batch mode available"
    - .claude/commands/review-translations.md contains "TRANSLATION FEEDBACK TEMPLATE"
    - .claude/commands/review-translations.md contains "Language: [ISO code"
    - .claude/commands/review-translations.md contains "Step 7 — Batch mode detection"
    - .claude/commands/review-translations.md contains "7a-single" (renamed existing section)
    - .claude/commands/review-translations.md contains "7a-batch" (new section reference, added in Task 2)
    - The existing Step 7 prompt "Would you like to give feedback on this review?" is still present
    - The existing 7b, 7c, 7d, 7e sections are unmodified
  </acceptance_criteria>
  <done>Step 7 has a session-independence note, an employee collection template, and batch format detection that branches to batch mode or falls through to the existing single-item flow. The existing 7a is renamed to 7a-single.</done>
</task>

<task type="auto">
  <name>Task 2: Add routing classification and conflict-aware block list display (7a-batch)</name>
  <files>.claude/commands/review-translations.md</files>
  <read_first>
    - .claude/commands/review-translations.md (re-read after Task 1 modifications)
    - .planning/phases/06-batch-feedback-routing/06-CONTEXT.md (decisions D-04, D-05, D-06, D-11, D-12, D-13)
    - .planning/phases/06-batch-feedback-routing/06-RESEARCH.md (Pattern 2: Routing Classification, Pattern 3: Block List Display, Pattern 4: Conflict Check Per Item)
    - config/label_patterns.json (subject_variable_usage_rules section structure)
    - config/tone_guidelines.json (formality_rules section structure)
    - config/Variables.csv (first 5 lines to see @TPL_ format)
  </read_first>
  <action>
Add a new `### 7a-batch` section to `.claude/commands/review-translations.md`, placed AFTER the batch detection section from Task 1 and BEFORE the existing "Would you like to give feedback?" prompt.

This section contains three sub-steps: parsing, routing, and display.

**7a-batch — Parse, route, and display**

```
### 7a-batch — Batch routing analysis

When batch mode is detected, process the input as follows:

#### Parse batch items

Split the user input into individual items by detecting `Language:` boundaries. For each block:
1. Extract the language code (trim whitespace, accept codes like `es_AR`, `ar`, `de`).
2. Extract the issue text (everything after `Issue:` until the next `Language:` or end of input).
3. If a language field contains multiple comma-separated codes (e.g. `Language: ar, he`), split into separate items — one per code — with the same issue text. Announce: "Item #N applies to [K] markets — creating [K] entries."
4. If a block is missing a `Language:` or `Issue:` field, skip it and warn: "Skipped malformed block: [first 50 chars]..."

#### Route each item

For each parsed item (language, issue_text), determine the routing destination using this decision tree:

1. **Variable mention check**: Does issue_text mention a `@TPL_*@` variable name?
   - YES: Read `config/Variables.csv` and check if the variable exists (search for the exact `@TPL_*@` string in the first column).
     - Variable NOT in Variables.csv → destination: `Variables.csv (flag only)`. Output: `⚠️ Variable @TPL_X@ may be missing from Variables.csv. Verify against BO before adding manually.` (per D-11/D-12). This item does NOT appear in the confirmation batch — it is informational only.
     - Variable IS in Variables.csv AND issue is about which variable to use, correct placement, or per-language variable rules → destination: `label_patterns.json`. Write target: `subject_variable_usage_rules.[VARIABLE_NAME]`.
     - Variable IS in Variables.csv AND issue is about translation quality around a variable (grammar, phrasing) → destination: `corrections_log.json`.
   - NO: continue to step 2.

2. **Formality/tone check**: Does issue_text mention formality, register, tone, or an address form (vos, du, tu, vous, Sie, usted, Lei, informal, formal)?
   - YES → destination: `tone_guidelines.json`. Write target: `formality_rules` (add/move language between `informal_standard_languages.languages`, `formal_vous_languages.languages`, or add to `market_notes`). Also write a corrections_log.json entry if a `rule_extracted` can be derived.
   - NO: continue to step 3.

3. **Default** → destination: `corrections_log.json`. Write as a standard 8-field entry via 7b.

#### Conflict check per item

For each item that has a writable destination (NOT Variables.csv flag-only items), run the same conflict check as 7a-single:
- Read `.claude/commands/review-translations.md` Steps 4c and 3
- If destination is `label_patterns.json`: check `subject_variable_usage_rules` for contradictions with the new rule
- If destination is `tone_guidelines.json`: check `formality_rules` for contradictions
- If destination is `corrections_log.json`: check existing entries for same language + same issue_category for semantic contradiction

Mark each item as `conflict: none` or `conflict: ⚠️ Conflicts with [file] [section] — "[existing rule text]"`.

#### Display block list (per D-04)

Present ALL items as a numbered block list:

```
Batch analysis complete — [N] items parsed:

#1 — [language_code]: [brief issue summary, max 10 words]
  → Routes to: [corrections_log.json | label_patterns.json | tone_guidelines.json | Variables.csv (flag only)]
  → Rationale: [one line — what will change and where]
  → Conflict: [none | ⚠️ Conflicts with [file] [section] — "[existing rule text]"]

#2 — [language_code]: ...
  ...
```

After the block list, show the confirmation prompt (the apply logic is implemented in plan 06-02):

```
Enter the item numbers you want to apply, separated by commas (e.g. 1, 3).
Items marked ⚠️ must be resolved before they can be included.
Items not listed will be discarded.
Variables.csv flag-only items are informational — they cannot be applied.
```
```

**Important implementation notes:**
- The `notification_type` field for batch-sourced corrections is `"batch-feedback"` (not tied to a specific CSV review).
- Per D-05, items with conflicts are displayed but excluded from the confirmation set — the user cannot type their number until the conflict is resolved.
- Per D-13, Variables.csv items are flag-only and never appear in the confirmation set.
- The block list display ends here for plan 01. Plan 02 adds the confirmation parsing and write execution.
  </action>
  <verify>
    <automated>grep -c "7a-batch" ".claude/commands/review-translations.md" && grep -c "Routes to:" ".claude/commands/review-translations.md" && grep -c "Rationale:" ".claude/commands/review-translations.md" && grep -c "Variables.csv (flag only)" ".claude/commands/review-translations.md" && grep -c "batch-feedback" ".claude/commands/review-translations.md"</automated>
  </verify>
  <acceptance_criteria>
    - .claude/commands/review-translations.md contains "### 7a-batch" section header
    - .claude/commands/review-translations.md contains the routing decision tree with all 3 steps: variable mention check, formality/tone check, default
    - .claude/commands/review-translations.md contains "Routes to:" in the block list format
    - .claude/commands/review-translations.md contains "Rationale:" in the block list format
    - .claude/commands/review-translations.md contains "Variables.csv (flag only)" for read-only routing
    - .claude/commands/review-translations.md contains the confirmation prompt text "Enter the item numbers you want to apply"
    - .claude/commands/review-translations.md contains "batch-feedback" as the notification_type for batch-sourced entries
    - .claude/commands/review-translations.md contains "subject_variable_usage_rules" in the routing logic
    - .claude/commands/review-translations.md contains "formality_rules" in the routing logic
    - The existing 7a-single, 7b, 7c, 7d, 7e sections are still present and unmodified
  </acceptance_criteria>
  <done>Step 7 has a complete 7a-batch section: batch items are parsed from Language+Issue format, routed to one of four destinations via the decision tree, checked for conflicts against existing config, and displayed as a numbered block list with routing rationale and conflict status. The confirmation prompt is shown but apply logic is deferred to plan 02.</done>
</task>

</tasks>

<verification>
After both tasks:
1. The full Step 7 has two parallel paths: batch mode (7a-batch) and single-item mode (7a-single through 7e)
2. The batch detection logic correctly distinguishes Language+Issue format from #N report numbers
3. The routing decision tree covers all four destinations: corrections_log.json, label_patterns.json, tone_guidelines.json, Variables.csv (flag only)
4. Conflict detection references the same sources as the existing 7a (now 7a-single)
5. The block list display format matches D-04 exactly
6. The employee collection template is present and simple enough for non-technical use
7. The existing single-item flow (7a-single through 7e) is completely unmodified except for the 7a header rename
</verification>

<success_criteria>
- A user can paste Language+Issue formatted feedback at Step 7 and see batch mode activate
- Each item shows a destination, rationale, and conflict status in block list format
- The routing correctly distinguishes variable issues (label_patterns), tone issues (tone_guidelines), unknown variables (flag only), and general corrections (corrections_log)
- The existing #N single-item feedback flow works exactly as before
- The employee collection template is visible in the skill
</success_criteria>

<output>
After completion, create `.planning/phases/06-batch-feedback-routing/06-01-SUMMARY.md`
</output>
